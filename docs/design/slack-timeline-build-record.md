# AIO-1170 — recoverable build record

State: specification accepted; Opus implementation has not started. This record contains sanitized recovery facts, not credentials, private message content or a production-resolution claim.

## Branch and scope

- Branch: `codex/aio-1170-slack-timeline`.
- Contribution base and intended PR target: `origin/staging`, commit `d1f0c088`; remote base re-fetched and unchanged at final specification preparation.
- Latest committed spec-review checkpoint before this record: `6fcd20ef9cc574f749225ea2eb120620a932f896`.
- Canonical specification: `docs/design/slack-timeline-reliability.md`.
- Accepted specification SHA256: `6d0085bf078e16fce0accf65ee06678b55a27b29f2e2a8bf85775d4f6c8dedba`.
- Owner ticket: AIO-1170, verified In Progress. Overlapping Slack scope was removed from AIO-1166 by the coordinator; other-provider work remains separate.
- No merge, deployment, production repair, live test-message posting or pending-proof draft PR is authorized by this build record.

## Specification review history

| Stage | Snapshot | Outcome and disposition |
|---|---|---|
| Guidance checkpoint | `df333a8a` | Staging destination, carried authorization and durable/remote backup/usage monitoring instructions; bounded local documentation review found no actionable issue. |
| Fable r1 | commit `7ccc7a26`; SHA256 `b7b666e67bcbab6f3fb21c5630778dbc5b1b306e17a2cdfe41e3b465af3ebd73` | BLOCKED. Astra addressed publication/migration gate, unchanged generation churn, continuation UI, capacity gating, identity writer cutover, and supporting medium/low items. Hardcap substitution and generic unfenced social-job reuse were rejected. |
| Fable r2 | commit `2c76dd99`; SHA256 `7691f03bd2d27934b884d0eeb48bdd9e243b35d404c1bd2ae3dbcb9478968ec2` | BLOCKED narrowly. All r1 architecture blockers resolved. Added direct timeline/credit identity-reader cutover and FTS prefix contract; clarified identity cold rebuild, reserved calls, health cadence and migration details. |
| Fable r3 | commit `6fcd20ef9cc574f749225ea2eb120620a932f896`; SHA256 `a8fcf0b60363abf23666965cd595c503ba2902c040a572b7b1a643709be45e93` | READY, no confirmed blockers. Verified H1/H2 resolved and reviewed identity-cache, rate reservation and health changes. |
| Astra final precision | accepted SHA256 above, based on r3 READY | Pinned the r3 nonblocking details: generation storage/hit validation/salvage, unchanged auto-sync, oracle absent-vs-empty semantics, transient metadata status, scheduler health/timer liveness, interactive validation, INSERT/UPDATE refusal and strict error handling. Clarified review cadence. These final wording details were not separately rerun through Fable; do not attribute the final hash directly to its r3 review. |

All three Fable reviews used verified exact Fable 5.1 through subscription authentication; coordinator verified successful process completion and no provider denials. No application code has been reviewed or attested by these specification reviews.

Accepted deterministic check: `aios spec eval docs/design/slack-timeline-reliability.md --no-llm --json` returned `SPEC_READY`, exit0, no findings, with the accepted SHA256 above. This is an offline readiness result, not live proof or permission to publish. The repository was dirty with documentation-only changes when evaluated.

## Baseline verification before implementation

Coordinator reported **74 passing baseline tests** on the staging-based worktree:

- 44 unit tests across Slack normalization, participant parsing, identity resolution and timeline grouping.
- 30 real-Postgres tests across Slack identity, work timeline and timeline cache, using the isolated worktree database.

These are baseline regressions, not tests of the proposed feature. They do not reproduce the user's incident, establish migration safety for new code, or attest reconstructed/modified code. Opus must add outcome-derived failing regressions and run the applicable isolated database/HTTP/UI checks after implementation.

## Accepted implementation invariants

One canonical message metadata ledger; workspace-qualified identities and paths with guarded in-place migration; existing item transaction publishes message state atomically; fair durable polling with per-method reservation; unchanged sync does not churn generations; shared lock-respecting credit; SQL contribution-time/visibility filtering before limits; retrievable same-day overflow; truthful failures and remap-safe cache; existing ACL and copied-staging protections preserved. Schema-only/pure-helper partial slices remain inactive. Identity writers/readers must cut over together; ambiguous legacy provenance blocks publication rather than duplicating or granting credit.

Build with subscription-authenticated Opus only. No fallback builder, API billing or paid-reset bypass. Preserve partial work at local/remote checkpoint intervals and review boundaries; required usage pause policy still applies. Routine recovery commits use the local pre-push review gate; Fable code review is for stable integrated snapshots and substantive reviewed fixes, not every small checkpoint.

## Outstanding acceptance and activation gates

- AC-01: representative reported missing message and visible control, with sanitized stage-by-stage source-to-UI trace. Source examples remain unavailable; no incident resolution claim.
- Installation metadata/capacity: coordinator read proved one enabled Slack integration selecting one channel. App rate category, full retained-root/reply-page demand and actual cycle throughput remain unverified. Conservative-budget fixture implementation is permitted; certified live activation is gated.
- AC-02–13: implementation and corresponding source→DB→identity→query/cache→API/UI tests remain unbuilt. This includes historical namespace repair, lock semantics, overflow, failures and authorization.
- AC-13 live People identity-context proof remains required.
- AC-14: 24-hour controlled soak after initial backfill in an authorized **non-copied** sandbox, including outage, identity correction and measured full cycle. Copied staging ingestion protections must not be bypassed.
- Any reproduced general access-invalidation dependency must be reported for coordinator adjudication; Slack fails closed and AC-11 remains unsatisfied until resolved.
- Required live gates block ready publication. No draft authorization is assumed. Reviewed implementation and passing fixtures alone cannot close this ticket.

The next action is coordinator verification/attachment of the accepted specification, a verified durable checkpoint, then the first inert Opus helper/test packet. Keep this record updated with implementation checkpoints, reviewed hashes, test evidence and remaining gates as work proceeds.

## Implementation checkpoint — pure evidence projection

Opus5 subscription implementation added an unused message-evidence helper,32 new unit cases, and optional bot_id typing. Active ingestion unchanged. Coordinator captured initial missing-helper failure before implementation;20 existing tests passed. After implementation,52 tests across3 suites and npm run typecheck passed at16:03ET2026-09-09. The delegated CLI required Bash approval; coordinator ran checks through normal execution controls. The prior baseline74tests are separate. Routine projection semantics and duplicate-conflict handling remain under Astra adjudication; this checkpoint does not certify integration or live acceptance.

### Packet1 corrections verified

Astra and independent local review identified incomplete author classification and unsafe first-observation conflict selection before integration. Opus replaced conflict selection with a typed whole-batch exception containing sorted IDs only, required explicit known-human flags, and added future-reevaluation and captioned-file exclusion assertions. Coordinator captured8failing regression assertions before fixes, then65passing tests across3suites and typecheck0 at16:14ET. The helper remains unused; later integration/live ACs remain pending.

## Packet2 schema checkpoint — verification pending fixture fix

Checkpoint3ad7035f adds slack_messages and slack_team_state only, plus schema invariants and architecture inventory. Existing items_team_id_id_idx and its deployed migration supply the composite FK; no redundant existing-table migration added. Coordinator fresh isolated database load succeeded;11/12real-Postgres tests passed including populated upgrade/replay. Team-cascade fixture failed because ingest seeded append-only audit rows. Astra approved a test-only minimal raw-SQL fixture, preserving the audit guard and shared lifecycle helpers. Docs drift87tables and typecheck passed. Independent local review found no HIGH/blocker for the inactive backup; remaining fixture verification is not claimed complete. No active writer/reader uses these tables.

### Packet2 fixture correction verified

Opus changed only the team-cascade fixture to minimal raw SQL rows and asserted no audit entries before deletion. Coordinator reran the isolated ledger suite:12/12tests passed at16:38ET, including from-zero/populated upgrade/replay. Audit protection and shared lifecycle helpers remain unchanged. Schema and production code are identical to3ad7035f; prior docs/typecheck checks remain applicable.

## Packet 3 — namespace parser verified

Checkpoint e0cbd9e0 adds an unused pure legacy/scoped path parser and validated scoped builders. Coordinator ran 74 tests across three suites, typecheck and docs drift: all passed at 16:54 ET. Tests were written before the helper; no initial-red run was captured for this new helper. Independent local review found no actionable correctness issue. Its timestamp dependency currently imports node:crypto transitively; extract a browser-compatible timestamp module if a future client component needs this helper. Current inspected data-browser is server-side and the helper has no active callers. Legacy path provenance, migration gates and publication remain pending.

## Packet 4a — pending-thread state verified

Recovery checkpoint 830fd276 adds inactive `slack_sync_threads` storage and session-bound enqueue, claim/reclaim, progress checkpoint and retry release primitives. There is no terminal acknowledgement or publication path. All authority checks use the database clock, full scope, owner and lease generation. Coordinator ran the real-Postgres suite against a freshly initialized worktree-only database: **28/28 tests passed** at 17:14 ET on September 9, 2026, including competing claims, expired/replaced owner refusal, caller rollback and populated schema replay. Type checking and documentation drift checks passed; the tested source matches the recovery commit. The initial captured red was missing-module resolution, not a behavioral regression. Active ingestion and remaining acceptance gates are unchanged. Origin staging was refreshed at 17:15 ET and remains 11eb039b, whose unrelated staging-image publication changes were assessed previously.

Local checkpoint review of 93a7bf93 passed for inactive backup with two medium findings requiring resolution before integration: invalid error-code exceptions currently echo supplied values, and the SQL timestamp syntax cap is narrower than the shared parser for leading-zero seconds. Astra adjudication and Opus corrections are pending. This is not the integrated Fable or final Astra review.

### Packet 4a corrections verified

Opus resolved both medium findings in 3c0f09d2: rejected error categories no longer enter diagnostics, and exact leading-zero root timestamps survive enqueue and schema replay. Coordinator captured two failing timestamp assertions before the schema correction; no initial diagnostic failure was captured because that fix loaded before the test. The corrected source passed **29 database tests**, **95 parser/evidence unit tests**, and type checking at 17:25 ET. A comment-only Opus follow-up fixed a false table match in the documentation scanner; docs checks passed at 17:29 ET, and non-comment SQL was verified byte-identical to the tested version. No scanner bypass or invented inventory entry was added. Active ingestion remains unchanged; all integration and live acceptance gates remain pending.

## Packet 4b — inactive namespace gate

Checkpoint 1ec2f473 adds a default-blocked per-team/raw-channel namespace gate with revision invalidation and a transaction-held readiness read. No application function can produce ready state; provider authorization and the actual provenance migration remain separate dependencies. Coordinator captured an initial missing-module failure, then **24 real-Postgres tests**, type checking and documentation checks passed. A final team-isolation test correction at b7a7fcd9 also passed all 24 tests at 17:52 ET on September 9, 2026. Two additional populated schema replays preserved every column of ready/blocked structural fixtures for two teams in a scratch database within the worktree-only container; the scratch database was removed afterward. These fixtures do not certify real channel provenance.

Astra identified a remaining test gap: the current competing-reader test does not guarantee it observes a database lock wait before the writer commits. A narrow Opus test-only correction is required before integration; no production helper defect was confirmed. The initial inspection-only Opus invocation was stopped without source edits after repeated shell approval requests. Its replacement used file-only tools with coordinator-run verification; no implementation was lost and subscription authentication remained unchanged. Integration and live acceptance gates remain pending.

Local review of 023e54a1 passed for inactive backup with one medium storage-validation finding: joining workspace IDs with commas lets a single comma-containing element satisfy the SQL array constraint. The application codec rejects that value and grants no readiness. Element-safe SQL validation plus a negative database fixture are required before integration, alongside the already recorded deterministic lock-wait regression.

### Packet 4b corrections verified

Opus corrected the workspace-array constraint and strengthened the concurrency test in 7dd6d2c0. Coordinator captured the new array regression failing against the prior isolated database constraint, then **25 real-Postgres tests**, type checking and documentation checks passed at 18:06 ET on September 9, 2026. The concurrency case now observes the writer in the reader's `pg_blocking_pids` before permitting commit. Two valid populated schema replays preserved every field for both team fixtures. A separate replay over the deliberately malformed regression fixture failed with SQLSTATE 23514 on the expected constraint and preserved the complete row; no automatic data repair was performed. The schema edit preceded the regression edits in this follow-up, so tests-first ordering is not claimed. All source matches the verified checkpoint; namespace activation and live acceptance remain pending.

Focused review of a47aae84 passed for inactive backup and confirmed the array and lock-wait fixes. One medium test cleanup issue remains: if the writer fails before its startup signal, the reader can wait on that unresolved signal and mask the cause with a timeout. A narrow test-only correction is queued for the next Opus slice before integration; the passing normal lock-wait result and production helper are unaffected.

## App identity bootstrap specification checkpoint — September 9

Packet4c implementation revealed a feasibility gap before coding: standard bot auth.test can omit app_id. Astra clarified same-token bots.info lookup for the exact authenticated bot, with a shared conservative workspace bootstrap budget. Fable5.1 returned READY for candidate58433486/spec26de4eb6166ba4d56be566c474793e0ee6abb0ed081ab9de4001ae97ab2f8d6f. One parent-directory Glob was denied; the exact authorized handoff listing and requested prompt read then succeeded. Coverage was checked from the transcript; no denied read was treated as evidence.

Astra accepted the three nonblocking precision findings: effective-token cache fingerprint including env fallback, actionable users:read fallback diagnostics, and unbound setup validation deferred until queued binding. These final precision sentences were not separately rerun through Fable. Final canonical spec SHA25619bcdf6edd9ca3bff2b39e69cf7284952dcf4879d84bbbafe67fb2ae1f2e3f14 passed deterministic SPEC_READY and was attached in full to AIO-1170 before further implementation. The complete canonical content and unchanged original request were verified after Linear formatting normalization; AIOS CLI verify-desc confirmed byte-identical readback SHA256aded7c241bf4723becc50eda81c513f5de20040e121a93d0b7c9af6b5f61b678.

No application behavior changed in this checkpoint. Integrated Fable code review, fresh Astra code review and live acceptance gates remain incomplete. PR target remains staging; no main push, merge or deployment. Packet4c next supplies durable method budgets and a one-request adapter, including the previously recorded gate test cleanup.

## Durable Slack requests — initial verified checkpoint

Subscription-authenticated Opus5 implemented the inactive method-budget and single-request adapter plus request tests and the gate-test early-writer-failure cleanup. Local partial checkpoint0208d979 preserves the initial source; c8d78b9e captures the tests and documentation. At c8d78b9e, the coordinator ran123 unit tests across3files and50 isolated real-Postgres tests across2files, all passing, plus typecheck and documentation drift checks. Only the dedicated worktree test container was recreated to load the schema. No initial failing feature test was captured; feature implementation preceded those tests, so this record makes no tests-first claim. The existing writer-failure test cleanup passed as part of the database suites.

Four accepted coordinator/Astra findings remain for the next Opus correction: verified-scope bots.info can bypass the required shared workspace refresh bucket; a valid48-hour Retry-After is shortened by a24-hour clamp; an arbitrary provider error matching the category regex can leak into diagnostics; and HTTP500 with an ok:true body can be reported as success. These new modules still have no active application caller. Existing tests do not discharge those counterexamples. The next correction must add meaningful regressions and preserve pending request allowance during schema replay.

No integrated Fable code review or fresh final Astra review has run; no acceptance criterion or live incident resolution is certified. Canonical accepted spec19bc remains fully attached in AIO-1170. Remaining work includes integrated discovery, publication, identity-context attribution, timeline/cache/UI, historical repair and required live gates. All pushes remain feature-branch backups for a staging-targeted PR.

## Request correction recovery checkpoint

Opus corrected all four initial request findings at b6494d56. Coordinator verification passed133 unit tests and57 real-Postgres tests, including populated bucket consolidation and repeated schema replay, plus typecheck. The documentation guard initially failed because a SQL comment was parsed as a table name. Actual failing baselines were captured first: unknown-code/HTTP unit cases, verified bots.info and48-hour database cases, and old-schema replay leaving obsolete rows. Missing new helper exports in some early unit failures are not represented as behavioral proof.

Astra found one remaining gap in the exceptional unrepresentable Retry-After path: an immediate blocked result left the shared bucket available after60seconds. Opus is adding a durable categorical block, preserving it through consolidation and returning blocked with zero HTTP for all later callers. Three new unit regressions failed against the prior code before these edits. The SQL comment has now been corrected and the docs guard passes. Current marker/schema/transport changes are a partial, unverified recovery snapshot; the Opus worker was briefly paused to make this remote backup stable and will resume from the same session. Previous passing results do not attest these newer changes.

The last verified remote checkpoint before this backup was47deda72. Newer local changes were preserved while the docs guard prevented backup; no guard was bypassed. All code remains inactive, with no application runner or publication wired to the new helpers. Integrated Fable/final Astra review and live acceptance gates remain outstanding.

## Request dependency verified after durable-block completion

The stable partial checkpoint77046abd was independently reviewed and pushed, then the same paused Opus process resumed. Final source33d48386 passed135 unit tests across3files and64 isolated real-Postgres tests across2files, plus typecheck and documentation checks. The database suite covers the real cross-caller block after the original deadline, propagation of marker-write failure, an upgrade from a table without blocked_reason, and preservation of any source/destination block through repeated populated consolidation. The three unit regressions had failed against the previous implementation before these edits.

The previously recorded initial request findings and the shared-block gap are corrected and tested. The single-request adapter is the only library importer of the budget module; neither has an active runner, admin or UI caller. This completes the inactive request dependency, not source discovery or incident resolution. The next slice connects real integration/config/effective-token binding, reserved provider requests, channel leases and atomic root enqueue/frontier progress through one internal discovery entrypoint. Publication, identity-context integration, timeline/cache/UI and live gates remain outstanding. Integrated Fable and fresh Astra code reviews remain required before a ready PR.

## Discovery schema and contract backup

Local schema checkpoint81efca81 loaded successfully in the dedicated isolated database and preserved all38 request-budget database regressions. Checkpoints b919151f and63548a71 add real encrypted synthetic integration fixtures and bootstrap, history/recovery and unit contracts before the discovery module exists. Baseline discovery suites failed collection on the missing module, with zero behavioral assertions executed; the inactive-wiring guard passed. These results are not a completed discovery implementation or an assertion-level red baseline.

The sole subscription Opus5 worker was briefly frozen for this stable review and backup, to resume in the same session afterward. Latest fresh Claude usage at19:54ET was61% session,72% weekly and64% Fable, credits off; no quota pause applies. Recovery remains from the same durable worktree and accepted19bc spec. A read-only integration plan pins existing shared identity/People context and timeline query/cache/pagination seams for later work. No legacy runner activation, ready PR, main push, merge or deployment. Required integrated reviews and live gates remain pending.

## First integrated discovery pass — incomplete

Opus5 completed the first discovery pass with no permission denials; final sourcefe0cb135 was preserved after intermediate checkpoints743a57dc and3636bd1a. The real integration/binding, public metadata, single history page and atomic root/frontier path is reachable internally and remains excluded from active callers. Coordinator verification on the clean snapshot passed52 unit tests across3files and126 isolated database tests across5files. Typecheck fails with three accesses to category on a union that still permits success; this is not a passing build.

Astra accepted further correctness fixes before discovery acceptance: the initial partial page incorrectly certifies a completed interval and discards continuation; metadata acceptance needs a channel-scoped attempt fence to prevent an old public response overwriting a newer private result and to refuse revoked in-flight history; deleting an integration must clear both binding fields without losing progress. The terminal interval test must compare the exact request latest timestamp rather than only syntax. Metadata cadence must be explicit/configurable, with live capacity/freshness evidence still pending. These defects are preserved as inactive WIP for the next subscription Opus correction. Passing fixture tests do not discharge them. No integrated Fable/final Astra review, ready PR, main push, merge or deployment.

## Claude usage cutoff — durable partial correction

At20:40ET September9 the authoritative Claude provider event reached90% session usage. The coordinator terminated the verified corrective Opus worker and its two child processes and verified all three absent; the final event at termination was91% session/76% weekly, no overage. The provider reports session reset at23:30ET. The last interactive Fable reading was66%, with weekly/Fable reset September15 at07:59ET. No builder fallback, API billing or credits were used. The stopped CLI returned143 from termination, not a completed implementation result.

All partial work is preserved in local checkpointsd29df337,76e2eb49,9e7c0c35 andda41bcc3: new URL/cadence unit contracts, metadata/deletion/replay database regressions, and incomplete channel-state corrections. No source schema or orchestrator correction from this pass has landed yet. The unit baseline failed6new tests because exports/constant are absent (11existing pass), not behavioral proof. The database baseline reproduced late-public-over-newer-private exactly; other failures were separately identified as predelete fixture state, history claim initialization comparison and absent upgrade block. These newer changes have not passed verification and do not inherit earlier green results. Resume from this exact partial state with the accepted corrections and previous spec; do not reconstruct or redo the valid request dependency.

The planned next usage check is21:40ET, then hourly if still blocked; actual fresh below90 evidence is required before restarting subscription Opus. Read the durable handoff for the scheduled automation and latest state. Scope remains Slack discovery correction followed by publication/identity/timeline integration and required reviews/live gates. No ready PR, main push, merge or deployment.

## September 19 recovery — Slack discovery correction checkpoint

The user resumed AIO-1170 from the accepted specification and explicitly authorized Sol or Terra for building, with a 40% Codex weekly usage ceiling and GitHub checkpoints at least every 30 minutes. The existing seven-file uncommitted patch was preserved byte-for-byte from the prior worktree, then completed in a separate writable checkout. This section supersedes the older Opus-only builder instruction for this resumed session; it does not change the accepted product specification.

Sol completed the pending discovery corrections: partial initial history retains its cursor without prematurely certifying an interval; metadata attempts fence stale responses and non-public proof revokes active history acceptance; integration deletion clears both binding fields while preserving scan state; terminal interval checks use the exact requested upper bound; and metadata cadence is configurable. The saved-token rotation regression now checks preservation before a later legitimate history request extends the frontier. A subsequent Sol pass fixed two local review findings: failure results return the persisted binding state, and malformed `has_more` values fail closed rather than marking a page terminal.

Staging was fetched at `0c11d81a` and merged into the feature branch at `1eb58cfa`; the only content conflict was `AGENTS.md`, resolved to the newer staging guidance. The pre-push local review found no HIGH/blocker, and its MEDIUM/LOW findings were both fixed. After staging integration and fixes: 153 focused unit tests, 41 isolated Postgres tests, typecheck and docs drift passed. The accepted spec is unchanged. This is still a WIP discovery checkpoint: publication, identity and timeline integration, full acceptance verification, Fable code review, fresh Astra final review and authorized live gates remain outstanding. Staging is the requested merge target; no ready PR or merge is claimed.

### Inactive replies hydration packet

Terra laid down source-owned `slack_thread_snapshots` storage and an unreachable one-page `conversations.replies` hydrator. Sol corrected the staged generation, rootless continuation, expired-snapshot restart, page/checkpoint atomicity, retry deadlines and cursor-history behavior. A local review then identified three HIGH issues: complete snapshots were reclaimed and refetched, the due selector could claim another workspace's row, and conflicting observations of one message ID were silently deduplicated. These were fixed before checkpoint: due selection is workspace-scoped and skips a live complete snapshot; conflicting content restarts the incomplete staged scan; a page write refuses an expired predecessor. The source still has no runner, publication or terminal acknowledgement path.

At the reviewed candidate, the focused hydration unit checks, typecheck and docs drift passed; the isolated real-Postgres thread-state suite passed **40/40**, including multi-page restart, same-claim concurrency, stale lease, complete snapshot, workspace selection, conflicting message and expiry-during-HTTP cases. A broad `npm test` run before the final fixes reported 6,173 passes and two failures: an NDA gate long-history fixture timed out at 30 seconds, and the staging refresh FK-closure inventory omitted pre-existing Slack binding/budget/channel tables. These failures remain open and the broad run does not attest the final diff. Required integrated Fable/Astra reviews, active publication, remaining acceptance criteria and live proof remain pending. Codex weekly usage was 2% at the last checked reading, below the user’s 40% cap.

### Staging refresh and NDA gate follow-up

The staging refresh exclusion inventory now includes the three integration-owned Slack tables required by its FK-closure guard, with the reason recorded in the operations runbook. The long-history NDA fixture has a 60-second test budget after taking about 30 seconds in isolation. Together, the two focused guard files passed **70/70** tests, and docs drift passed. A second broad local run was stopped after unrelated staging process tests timed out under parallel child-process contention; it is not a passing full-suite result. The local pre-push reviewer found no HIGH/blocker in these follow-up changes. The GitHub PR remains draft, targeted to staging, with publication and acceptance work pending.

### Inactive message ledger packet — September 19

On `codex/aio-1170-resume`, an inactive source-owned helper now reconciles `projectSlackMessageEvidence` for one verified complete team/workspace/channel/root/item snapshot using the caller's `TransactionSession`. It rejects partial or inconsistent projections, confirms the item belongs to the team and retains one thread binding, serializes same-team publishers, upserts exact Slack message IDs and microsecond instants, and marks only absent rows from a complete snapshot deleted. Identical revisits refresh `last_seen_generation` without bumping `data_generation`; content/verdict/deletion changes bump once per publication. An indexed team-generation read returns zero for a missing row and propagates SQL errors, and an atomic identity-generation primitive is available for later identity writers. No DDL or migration change was needed. No active ingestion, job, timeline or API call site imports the helper.

Verification: `npm run test:datamechanics:iso -- test/datamechanics/slack-message-ledger.datamechanics.test.ts test/datamechanics/slack-ledger.datamechanics.test.ts` passed **22/22** tests across two real-Postgres files, including exact timestamp identity, changed verdict with unchanged raw hashes, deleted/restored evidence, rollback, observed lock blocking for both identical and competing-item publishers, tenant isolation, and SQLSTATE 42P01 on a failed read. `npm run typecheck`, `npm run check:docs`, targeted ESLint on the three touched TypeScript files, and `git diff --check` passed. The helper and tests were added before their first test run, so no tests-first failure is claimed. An independent local pre-push review found no HIGH/blocker. Its integration caveat remains: `complete` is caller asserted, so the eventual publisher must derive it from the fenced staged snapshot and recheck scope/config. No active publication, PR readiness or merge is claimed.

Remaining: the publication gate and `ingestItem` transaction integration must verify the staged snapshot, job fence and config revision, call this helper in both body branches and acknowledge the job atomically. Identity writers, the credit oracle, timeline/cache consumers, historical repair, integrated Fable/Astra reviews and live acceptance gates remain separate work. An empty ledger is not yet authoritative evidence for active readers.

### Historical Slack path migration replay hardening — September 19

The pre-push review found that the historical migration still deleted an occupied ID-keyed target, losing independent history. `postgres/migrations/20260725180000_slack_paths_by_channel_id.sql` now selects only the exact original valid Markdown form, `slack/<lowercase-channel-slug>/<positive-seconds>.<1–6-digit-fraction>.md`; malformed three-segment values are not inferred or rewritten. A full schema replay leaves later workspace-qualified `slack/<workspace>/<channel-id>/<root-ts>.md` items outside the candidate set even when their frontmatter `channel_id` differs in case. Before any update, an occupied target now raises the sanitized actionable `P0001` error `slack path migration collision: operator resolution required before replay`. It deletes neither item nor graph episode; verified-provenance operator repair is required. The temporary replay view is removed before commit, so a no-collision second execution on a pooled connection remains a true no-op.

Verification: the real-Postgres fixture covers a successful two-version legacy/scoped replay, malformed-path preservation, and a collision with two independently versioned items plus `slack_messages` and `graph_episodes` evidence. The collision assertion requires the exact sanitized error and snapshots every dependent fixture before and after rollback. The coordinator reran `npm run test:datamechanics:iso -- test/datamechanics/slack-path-migration-replay.datamechanics.test.ts` with the approved Docker access after Terra's sandbox denial: **2/2 passed**. `npm run typecheck`, `npm run check:docs`, and `git diff --check` passed after the review correction.

Remaining namespace work is unchanged: provenance-backed historical repair must populate the per-team/raw-channel blocked-or-ready publication gate; new Slack publication must lock that gate before canonical path locks; and the scoped path parser, cleanup/purge, library/data browser, retrieve and both FTS legs still need their coordinated consumer integration and acceptance coverage. No ingestion, UI or active publication path changed in this packet.

### Inactive new-channel namespace readiness producer — September 19

GPT-5.6 Sol is the sole implementation writer for this bounded transaction/concurrency packet on `codex/aio-1170-resume`, starting from clean `b0d7817c`. The accepted specification is unchanged. The producer in `lib/ingest/slack-namespace-gate.ts` takes only the caller's transaction session and team/raw channel. It locks the gate first; resolves one current selected integration through the existing authoritative config/effective-token reader; requires its current verified binding and matching public `slack_sync_channels` proof; blocks on any team legacy or ambiguous Slack path and on an already scoped item for this channel; then writes a durable `slack_namespace_readiness_proofs` row and sets gate ready at the same revision. A short `items` SHARE lock prevents concurrent legacy item inserts during the empty scan and through that transaction's commit. It does **not** prevent the legacy Slack writer from inserting after commit. Attended rollout must disable and drain old workers **before invoking this helper** and before scoped publication is activated. Any absence, ambiguous workspace, stale revision, changed token/config, missing public proof or existing legacy row stays blocked. No caller supplies a workspace, verified flag, or proof ID. The source-owned helper is inactive: no runner, publisher, UI or manual action imports it.

The proof table is additive in `postgres/schema.sql` and `20260919175000_slack_namespace_readiness_proofs.sql`; it records team/channel, gate revision, workspace, binding/integration, selected config revision, public check time, zero legacy rows and completion time. Invalidation clears the gate's current proof pointer and advances revision but retains the completed proof record for inspection. Historical channels with any legacy row remain blocked. Their verified in-place repair, including collisions/unknown provenance, is still unbuilt; this packet does not guess workspaces, repath or delete items. Active publication and all downstream namespace consumers remain separate work.

Before editing, the worktree and PR inventory found draft staging PR #714 on `codex/aio-1170-slack-timeline`, whose separate worktree has uncommitted discovery/schema edits. This packet changes only the resume branch; its new migration timestamp was unreserved among active worktrees and local migration files at inspection. PR #714 and the discovery worktree overlap `postgres/schema.sql` and the gate area, so integration must reconcile them explicitly before publication. The builder did not commit, push or edit the PR; the coordinator handles the user's requested GitHub checkpoints.

Verification of the pre-review snapshot: the isolated real-Postgres readiness and existing gate suites passed **36/36** tests across two files. They cover first ready, missing/claim-only refusal, verified binding and selected revision, public proof, historical/unrelated/unknown paths, workspace ambiguity, team isolation, rollback, observed concurrent invalidation lock wait, and populated schema/migration replay. The inactive-wiring guard passed **1/1**. `npm run typecheck`, `npm run check:docs`, targeted ESLint on changed TypeScript, and `git diff --check` passed. These results do **not** attest the later candidate-set race correction below. No live Slack channel, full integrated code review, historical repair, active publication, or acceptance/soak gate is claimed complete.

### Independent HIGH review correction — candidate-set insert race

The independent pre-push review found that the first candidate count could be overtaken by a second-workspace `slack_sync_channels` INSERT, because discovery does not take the namespace gate. The producer now treats that first count only as an integration locator. It locks the gate, then the integration, then takes a short `slack_sync_channels` TABLE SHARE lock and repeats the decisive candidate count before binding/channel verification. The table lock excludes discovery's INSERT/UPDATE RowExclusive locks through commit. This order follows discovery's integration-before-channel write transactions; taking channel TABLE SHARE before the integration lock would create a deadlock cycle with metadata acceptance. A separate no-integration discovery ensure holds only the channel table write lock, so the producer waits and recounts. The existing `items` TABLE SHARE follows the channel proof. These two table locks temporarily block writes to their respective tables **across all teams** for the remainder of this short caller transaction; SELECTs continue. There is no active caller yet.

Two deterministic real-Postgres regressions were added: one observes a second-workspace INSERT waiting on the producer's channel table lock via `pg_blocking_pids`, then checks the second workspace cannot receive ready and that re-preparation after invalidation blocks; the other observes the producer waiting on a discovery-style integration lock, inserts the second row before releasing that lock, and requires the post-lock recount to block readiness without a deadlock. The source-owned helper and schema comments now state the activation limit: table SHARE protects only the in-transaction empty scan. A ready row cannot stop the legacy writer after commit; attended rollout must disable and drain old workers **before invoking this helper** and activating scoped publication.

Verification of this correction: the builder's sandbox could not reach the Docker daemon (exit 126), and a disposable local PostgreSQL `initdb` attempt was denied at `shmget`. The coordinator ran the two focused suites with approved Docker access. The first run failed only on a test assertion that expected the two workspace IDs in the wrong lexical order; after correcting that assertion, the real-Postgres suites passed **38/38** across two files, including both new concurrency cases. `npm run typecheck`, `npm run check:docs`, targeted ESLint, `git diff --check`, and the source-not-wired guard (**1/1**) also passed. The inactive producer remains unapproved for activation until the old Slack workers are drained and the publication path is reviewed.

### Inactive `ingestItem` Slack publication packet — September 19

GPT-5.6 Sol, as the single implementation writer, continued from clean `codex/aio-1170-resume` at `290f4a54`. The accepted specification was read and not revised. `lib/ingest/index.ts` now accepts an optional branded internal Slack publication capability as a seventh argument. No current runner, scheduler, manual action, route or API payload supplies it. When absent, the existing ingest flow remains on its original path. With it, the existing `runContextTransaction` first locks the ready namespace gate at the supplied current revision and exact workspace, then revalidates the current selected integration/effective token, verified binding, selected channel and locked public channel proof. It locks the full thread owner/generation/unexpired lease and the same-generation complete unexpired staged snapshot before canonical old/new item path identity locks. A live legacy item, a scoped path in another project, or an unrelated item at the target path refuses publication.

The publication helper checks path and complete frontmatter/body rendering against the staged raw root and replies; its exact `source_ts` carries microseconds. The internal option captures an immutable copy of the claim, binding and author directory so a later provider read cannot retarget an in-flight transaction. It projects the raw snapshot through `projectSlackMessageEvidence` with missing/partial author directory records left unresolved. Both changed-body and unchanged-body branches call the one source-owned ledger reconciler in the same `TransactionSession`. The final fenced queue delete rechecks both lease and snapshot expiry after item and ledger writes; its FK cascade removes staging. A ledger, item or acknowledgement error rolls back the transaction. An identical revisit leaves `data_generation` unchanged, while an eligibility change on an unchanged body advances it. The source transaction has no provider request or nested transaction, and generic `23505` retry was not changed.

Verification for this packet: `npm run test:datamechanics:iso -- test/datamechanics/slack-publication.datamechanics.test.ts test/datamechanics/ingest-atomicity.datamechanics.test.ts test/datamechanics/items-author-attribution.datamechanics.test.ts` passed **28/28** real-Postgres tests across three files. After the immutable-option hardening, the focused publication Postgres file passed again **19/19**. Its cases cover create, changed existing item, unchanged item with changed verdict and identical revisit, exact ledger rows/generations, queue and staging acknowledgement, stale/reclaimed/expired lease, partial/expired/wrong-generation snapshot, public/config/selected-channel/binding/namespace revocation, payload mismatch, ledger sink failure, acknowledgement failure and expiry at final acknowledgement. `npx vitest run test/ingest-slack-publication.test.ts test/guards/slack-source-not-wired.test.ts` passed **2/2** before the new immutable-option unit case; final focused unit and guard results are recorded below. `npm run typecheck`, `npm run check:docs`, targeted ESLint on the four TypeScript files and `git diff --check` passed before that last hardening; final static checks are recorded below. A first combined Postgres run exposed that the SQL adapter returns `clock_timestamp()` as a string; the helper now converts and validates it before evidence projection. One later test setup initially omitted the binding state's required sanitized error code; correcting the fixture produced the final **28/28** result. No tests-first failure is claimed. A verified local copy of the uncommitted packet was retained under ignored `.context/aio-1170-publication-handoff/`; the builder did not commit, push, edit PR #714 or merge, per the user instruction.

Activation remains blocked: old Slack workers must be disabled and drained **before** the new-channel readiness helper can be invoked or this path made reachable. Historical namespace repair must establish provenance separately; this packet never guesses it. Active `slack-normalize` remains legacy until the coordinated namespace and consumer cutover. The future source worker must supply the branded option and complete payload from its verified provider reads; it is not wired here. Identity writers, credit/timeline/cache consumers, purge and path consumers, integrated independent reviews and live acceptance/soak evidence remain outstanding. This packet does not claim AIO-1170 acceptance completion.

Final checks after immutable-option hardening: focused unit plus inactive-wiring guard **3/3**, `npm run typecheck` exit 0, `npm run check:docs` exit 0, targeted ESLint exit 0 with no warnings, and `git diff --check` exit 0. The focused real-Postgres publication file passed **19/19** after the same change.

Independent pre-push review of the inactive publication packet found no HIGH/blocker and two MEDIUM integration requirements. First, its cross-project collision check locks preexisting rows but does not exclude a concurrent ordinary ingest creating the same scoped path in another project, because the current ingest identity lock includes project ID. The correction below closes that race at the shared ingest ingress. The transaction's existing cross-project check remains for direct or historical rows. Second, a changed rendered title or author display label with identical raw source evidence does not currently invalidate a timeline presentation cache. The cache/consumer cutover must add separate presentation invalidation as specified in §66 before this publication seam is reachable. That MEDIUM remains open; the correction does not prematurely bump semantic data generation.

### Independent MEDIUM correction — scoped path ingress reservation, September 19

From clean `e576e986` on `codex/aio-1170-resume`, the single implementation writer reserved only canonical `slack/<workspace>/<channel>/<root-ts>.md` paths at `ingestItem` entry, using `parseSlackItemPath` and `scopedSlackItemPath`. An ordinary ingest, whether it names the `slack` project or another project, now fails with an explicit validation error before the project upsert or graph pointer write. Legacy Slack paths and unrelated project paths remain accepted. Only an option actually issued by `slackPublicationOption` passes the entry check; `prepareSlackPublication` repeats that runtime brand check inside the transaction and still validates the claim, binding, ready namespace, staged snapshot and payload. A truthy cast or copied symbol cannot grant publication authority. The original transaction-local cross-project row check is retained for preexisting or direct SQL rows. No team-wide item-path lock, global uniqueness constraint, generic retry change or active runner was added.

Verification: `npx vitest run test/ingest-slack-publication.test.ts test/guards/slack-source-not-wired.test.ts` passed **4/4**. `npm run test:datamechanics:iso -- test/datamechanics/slack-publication.datamechanics.test.ts` passed **21/21** against isolated real PostgreSQL; the combined command adding `test/datamechanics/ingest-atomicity.datamechanics.test.ts` and `test/datamechanics/items-author-attribution.datamechanics.test.ts` passed **30/30**. The new database cases assert ordinary cross-project and ordinary Slack scoped writes leave no item, project update or graph pointer, and that legacy and unrelated paths still create items with pointers. Existing branded create/update/unchanged publication cases remain green. `npm run typecheck`, `npm run check:docs`, targeted ESLint on the four changed TypeScript files and `git diff --check` passed. The first sandboxed PostgreSQL attempt could not reach the Docker socket (exit 126); the authorized isolated Docker run above succeeded. No provider call, source activation, commit, push, PR edit or merge was made by this writer. Presentation invalidation remains the open MEDIUM integration requirement before this publication seam is reachable.

### Data page namespace consumer packet — September 19

From clean `377d5280698c483576e279388a952bc51eca40d4` on `codex/aio-1170-resume`, this bounded packet updates only the existing Data page grouping/feed reader and its focused tests. `lib/library/channels.ts` uses `parseSlackItemPath` for exact legacy paths and the canonical four-segment scoped shape; scoped keys contain both workspace and channel. Unrecognized Slack paths receive distinct inspectable keys rather than being guessed into a valid channel. Non-Slack first-two-segment grouping remains unchanged. `components/library/data-browser.tsx` selects by the parsed key, escapes PostgreSQL LIKE metacharacters in literal prefixes, and reads successive bounded batches until it has the requested number of matching visible rows plus one for Load more. Every batch retains the existing `visibleItems` posture filter and ENFB-1 membership-oracle ID filter. Scoped filenames display relative to the full scoped key.

Verification: `npx vitest run test/library-channels.test.ts test/library-data-browser.test.ts` passed **21/21** unit/contract tests across two files after review corrections. The DataBrowser mock contract covers two workspaces with the same channel ID, a separate legacy row, tier and membership visibility, a legacy prefix whose first SQL batch consists of scoped rows, Load more, the 500-item cap, and a non-Slack key containing SQL wildcards. `npm run typecheck` passed; `npm run check:docs` passed with 67 routes, 97 tables and 8 sources in sync; targeted ESLint on the four changed TypeScript files and `git diff --check` passed. These are local tests with a SQL-like query mock, not a real-Postgres or live UI certification. No new Slack runner was activated. The writer did not commit, push, edit a PR or merge; the changes remain in the working tree for coordinator review/checkpoint.

Independent pre-push review found no workspace/legacy leak or visibility defect, but identified a MEDIUM preexisting Load more loop at the 500-item feed cap and a LOW synthetic-key collision between malformed Slack and arbitrary non-Slack paths. The coordinator fixed both: Load more is suppressed at the cap, and the malformed-Slack key uses adjacent slashes that the non-Slack first-two-segment parser cannot emit. Both have regression cases; the final focused suite passed **21/21**, typecheck and targeted lint passed, and `git diff --check` is clean.

The coordinated path-consumer cutover is **not complete**. `lib/ingest/sources/slack-normalize.ts` still writes legacy paths; `lib/ingest/slack-cleanup.ts` and `lib/ingest/purge.ts` still need scoped/legacy mapping-aware cleanup and purge; `lib/query/retrieve.ts` and both recency/ranked legs in `lib/query/fts-search.ts` still need visible workspace-qualified prefix-set matching. Historical provenance repair, migration replay, integrated reviews and activation gates remain separate work. Do not activate scoped polling/publication based on this Data page packet.

### Retrieval channel-scope consumer packet — September 19

From clean `e38ce860` on `codex/aio-1170-resume`, the sole implementation writer changed the channel-scoped item retrieval legs. `lib/query/channel-scope.ts` resolves **all** current member-visible Slack identities advertising the requested channel name, plus exact legacy paths whose readable segment is that name even without channel metadata. It validates each with the shared exact legacy/scoped parser and builds distinct slash-terminated `slack/<channel>/` and `slack/<workspace>/<channel>/` prefix sets. A scoped workspace segment equaling the requested name is never treated as a channel. The lookup never selects one matching row or infers a workspace from an integration. The same resolved set now gates ranked FTS, its semantic-expansion FTS call, general recency and source recency. SQL escapes literal LIKE prefixes, checks a parser-shaped root filename so a legacy channel cannot absorb scoped workspace paths, and intersects membership-visible item IDs before ORDER/LIMIT. Non-Slack paths keep the readable second-segment name rule. A failed channel lookup throws an actionable error instead of resolving to an empty scope. Direct `rankedFtsSearch` calls with a string channel resolve the same set; null/no-channel callers retain their interface.

The isolated real-Postgres regression seeds canonical scoped paths by test-only raw SQL after ordinary legacy item creation; production `ingestItem` remains closed to ordinary scoped writes. It checks two visible workspaces sharing a raw channel ID, a separate legacy path without channel metadata, a workspace whose segment equals the requested channel name but advertises a different channel, an old display name under a selected prefix, another channel's exclusion, nine louder and newer invisible rows under a selected prefix, literal underscore matching, agreement of FTS and both recency legs, and an actionable lookup SQL failure. The older multichannel test's channel-scoped fixture now uses an exact legacy timestamp path rather than its generic arbitrary filename. The **final** focused real-Postgres run passed **30/30** across `channel-scope`, `multichannel-adversarial` and `access-enforce-retrieve`; focused `query-adversarial`, `query-recall` and `library-channels` unit suites passed **46/46**. `npm run typecheck` and `npm run check:docs` passed (67 routes, 97 tables, 8 sources). Targeted ESLint exited 0 with two pre-existing unused-symbol warnings in `retrieve.ts` and no errors. The first sandboxed Postgres attempt could not reach the Docker socket (exit 126); the authorized isolated Docker runs passed. This writer did not commit, push, edit a PR, merge or activate a Slack runner.

Independent pre-push review found three integration gaps. The shared predicate initially dropped the older `frontmatter.channel` arm for non-Slack rows with opaque path segments; it now accepts either that name or the readable second path segment, with a real-Postgres regression. A direct external-tier FTS helper call without a membership-ID set could initially return a team item under a resolved prefix. An independent skeptic found no current query-route exposure because both routes pass the membership oracle set, but the helper now also enforces `i.access='external'` in its null-ID mode, with a direct-call regression. The original combined test made wrong-workspace/channel controls invisible and let all visible rows fit general recency's eight-row cap. Those controls are now visible, and nine newer, same-channel non-Slack items displace old Slack rows from general recency; a source-scoped query must recover them through source recency. The reviewed exact diff passes **32/32** real-Postgres tests across the three named suites, **46/46** focused unit tests, typecheck, docs drift, targeted ESLint (the same two pre-existing warnings), and `git diff --check`.

This consumer packet does not complete the coordinated cutover. Legacy normalization, namespace-aware cleanup/purge, historical provenance repair, presentation-cache invalidation, integrated reviews and live acceptance/soak evidence remain outstanding. Old Slack workers must be disabled and drained before readiness preparation or scoped publication is made reachable.

### Scoped Slack cleanup seam — September 19

From clean `b7698740` on `codex/aio-1170-resume`, GPT-5.6 Sol first attempted inactive `storedScopedSlackThreads` and `purgeDeletedScopedSlackThreads` entry points. The active `lib/ingest/run.ts`, legacy `slack-normalize` writer, and legacy cleanup entry points remained unchanged. The scoped read was team-scoped, used the validated `slack/<workspace>/<channel>/` prefix with escaped SQL LIKE, and accepted only exact canonical scoped item paths with matching Slack source, kind, workspace, channel and root timestamp metadata. The deletion-diff attempt required each candidate's positive `threadExists` definite-gone result; Slack errors kept the row and emitted a notice. This deletion entry point was reviewed and discarded; the final packet retains only the read.

No durable, verified legacy-path-to-workspace mapping table exists in this branch. The retained read never matches three-segment legacy paths, regardless of a display segment or `channel_id`/`workspace_id` frontmatter. Legacy cleanup remains on the old active runner until the attended cutover. A future mapping and repair must preserve item identity and prove historical provenance before any legacy deletion or scoped migration. A future scoped worker must supply workspace from its verified token binding and channel proof, not from item frontmatter; it must be wired only after old workers are disabled and drained, namespace repair/gates are complete, and the coordinated source/consumer cutover is reviewed.

The first builder snapshot had a callable `purgeScopedPrivateSlackChannel` and an `expectedPaths` precheck outside the purge transaction. Independent review found two P1s: the private function removed only item rows while raw staged `slack_sync_threads` / `slack_thread_snapshots` and other channel state could survive, and a path change between precheck and graph retirement could retire a live moved item's graph before conditional DELETE returned zero. Review also found a P2: strict metadata filtering could silently spare canonical-path content in the confirmed-private namespace. A failed later item delete could additionally leave earlier deletes committed without audit or cache bust. The accepted correction removed the partial private-purge export and its test. A future full private purge must clear all channel state and raw staged data, and fail or suppress canonical rows whose metadata has drifted; no partial private purge is callable from this packet.

The second builder snapshot retained the scoped deleted-thread cleanup and added an `expectedPaths` branch to shared `purgeItemIds`: it required a transaction-capable client, locked exact team/item rows, checked paths, and performed graph retirement, conditional item deletes, audit and cache bust on the bound session before commit. Independent final review rejected this attempt. New **P1**: Graphiti deletion is an external effect and cannot roll back if a later database delete or commit fails; the item can remain live without graph facts, and automatic re-projection is not guaranteed. New **P2**: cache eviction before commit allows a concurrent reader to refill the cache from rows that the transaction has not yet removed. Transactional item rollback and row locks do not close either gap. The `expectedPaths` branch has been removed and shared `purge.ts` restored exactly to HEAD.

The rejected attempts added `test/datamechanics/slack-scoped-cleanup.datamechanics.test.ts` with real-Postgres cases for exact scoped reads and adversarial rows; confirmed deletion and version cascade; alive/failed confirmation; channel mismatch; path movement during provider confirmation; a second session's path update while graph retirement was paused; and whole-batch rollback after an injected later delete failure. Before review, the coordinator ran the four related real-Postgres files with **27/27 passing**, the four focused unit/guard files with **64/64 passing**, typecheck, docs drift, targeted lint, and diff check. Those results cover the first rejected snapshot only.

Second-attempt builder verification: `npx vitest run test/ingest-slack-deletions.test.ts test/slack-namespace.test.ts test/ingest-purge-meta.test.ts test/guards/slack-source-not-wired.test.ts` passed **64/64**; `npm run typecheck`, `npm run check:docs` (67 routes, 97 tables, 8 sources), targeted ESLint of the three changed TypeScript files, and `git diff --check` passed. The builder sandbox was denied the Docker daemon socket. The coordinator ran the four focused files against isolated real Postgres. Its first run had **27/28** passing: the rollback test expected paths in the wrong lexicographic order, while its actual assertion still showed both rows surviving. The coordinator corrected that test expectation; the final run passed **28/28** across four files. Those passing results do not attest the final code or resolve the external-effect and cache-order findings.

The final uncommitted packet exports **only** `storedScopedSlackThreads` with its private canonical scoped read. It contains no scoped deletion function or private purge, makes no change to shared `purge.ts`, and leaves the active runner unchanged. Deletion and concurrency tests for the discarded functions were removed; the retained scoped-read tests cover team, workspace, channel, malformed and legacy paths, metadata mismatches, and lookup errors. Full scoped cleanup requires durable retire intent/outbox, post-commit Graphiti cleanup with retry, cache eviction after commit, source-owned raw staging and private-channel purge, and verified legacy-path mapping before it can be made callable.

Final-packet builder checks: the four focused unit/guard files passed **64/64**; `npm run typecheck`, `npm run check:docs` (67 routes, 97 tables, 8 sources), targeted ESLint of the scoped-read implementation and test, and `git diff --check` passed. `lib/ingest/purge.ts` has no diff from HEAD. The coordinator's first final-code real-Postgres run had **24/25** passing because a new fixture used invalid `item_kind='note'`; after changing that fixture to the valid `artifact` enum, the final isolated run passed **25/25** across the scoped read, legacy Slack deletion, prefix purge and ID purge files. The earlier Postgres results above belong to the rejected deletion snapshots.

### Publication presentation-cache blocker — September 19

From clean `fba76325`, GPT-5.6 Sol inspected the existing publication and timeline cache contracts. The publisher can change a rendered author/title label while raw Slack message evidence remains identical. `data_generation` must remain semantic-only. A post-commit `bustTeamTimeline` is insufficient: `getCachedWorkTimeline` serves a fresh process-local entry without consulting Postgres, and serves a stale persisted row verbatim before its background refresh. A persistent cache-row purge would still leave fresh entries on other workers and could be overtaken by an in-flight rebuild on another worker. Deleting the local in-memory entry before a persistent write also permits a concurrent read to refill it from the old row. No current durable presentation signal is checked by timeline readers. Reusing `slack_team_state.updated_at` is ambiguous because it also changes on semantic/identity writes and is not a strict revision. Adding a new revision would define the generation-reader contract excluded from this bounded packet; an unread dirty flag or event row would not invalidate a reader. Production code is unchanged. Presentation cache and coordinated timeline reader cutover therefore remain blockers before scoped publication is activated.

Real-Postgres cases were added to `test/datamechanics/slack-publication.datamechanics.test.ts` for the independent publication invariants: rendered author-label and channel-name/title edits with unchanged raw message projection keep `data_generation` stable, and an identical revisit adds neither a version nor a generation bump; an injected acknowledgement failure rolls an author-label edit back while retaining the old item, ledger and pending job. The builder sandbox could not start PostgreSQL because Docker and local `initdb` were denied. The coordinator then ran the final publication file against isolated real Postgres: **24/24 passed**. `npm run typecheck`, `npm run check:docs` (67 routes, 97 tables, 8 sources), targeted ESLint, and `git diff --check` passed. No runner activation occurred in this packet.

Coordinator recheck of the final uncommitted diff: `npx vitest run test/ingest-slack-publication.test.ts test/guards/slack-source-not-wired.test.ts` passed **4/4**; typecheck, docs drift, targeted ESLint and diff check exited zero. The subsequent isolated real-Postgres run passed **24/24** in `slack-publication.datamechanics.test.ts`. The cache-generation/identity reader cutover, coordinated source and path-consumer activation, historical namespace provenance repair, old-worker drain, integrated reviews and live acceptance/soak evidence remain outstanding; the inactive publication seam must stay unreachable.

### Durable presentation revision prerequisite — September 19

GPT-5.6 Sol continued from `fba76325` as the sole sequential writer, preserving the pre-existing uncommitted publication tests and record edits. `slack_team_state` now has an additive, nonnegative `presentation_generation bigint` with zero default and a replay-safe timestamped migration. The source-owned generation read returns all three counters, treats an absent row as three zeroes and propagates SQL read failures. In inactive issued Slack publication only, `ingestItem` passes the prior item row obtained under its path/item lock. After the item write and ledger reconciliation, the source-owned helper reads the item actually stored in the caller's `TransactionSession` and bumps presentation once for a first scoped item or a changed stored actor, author, title or channel label. An identical revisit and a reply-body edit with unchanged rendered labels do not bump it. The bump shares the transaction with the ledger and final job acknowledgement; either failure rolls it back. The semantic-only `data_generation` rule remains separate. Ordinary non-Slack item publication and cache readers were not changed.

The existing real-Postgres publication suite now asserts the initial presentation bump, same-raw-evidence author and channel/title changes, identical revisits, a reply-body semantic edit with stable title/author labels, and rollback on ledger and acknowledgement failures. The ledger suite pins absent-row zero and independent counter behavior. The populated upgrade/replay suite drops the new column to model a two-counter installation, applies its migration twice, preserves the old counters, then confirms full schema replay retains a nonzero presentation revision. The builder's isolated Docker attempt was denied. The coordinator's first run reused an older test container without the new column and failed with SQL 42703; after recreating only this worktree's disposable Postgres container and loading the new schema, the focused publication, ledger and message-ledger suites passed **47/47** across three files.

Focused checks on this revision passed: `npm run typecheck`, `npm run check:docs` (67 routes, 97 tables, 8 sources), targeted ESLint, `git diff --check`, and the publication/inactive-runner unit suites **4/4**. A verified ignored local diff and migration copy is in `.context/aio-1170-handoff/presentation-checkpoint`. The generation-aware timeline reader and cross-worker invalidation are still **incomplete**, so this writer prerequisite does not close the user-facing stale-label cache issue. Integrated reviews remain pending.

Coordinator follow-up ran `context-transaction-retry`, publication and inactive-runner unit suites **10/10**, plus the real-Postgres item-context serialization and ingest-atomicity suites **55/55**. An independent pre-push reviewer found no actionable defect in the inactive presentation-revision packet, including its locked prior item, transaction ordering and replay-safe migration. It flagged the known activation requirement: cache readers must validate revisions on every hit and protect concurrent rebuild publication. A reply-only author display rename can change transcript text and participant labels without advancing this root/title presentation revision; the current timeline does not consume those participant labels, so the future cache cutover must revisit that field if it renders them.

### Generation-aware timeline cache reader/writer packet — September 19

From clean `de05c90f` on `codex/aio-1170-resume`, with user-provided staging merge base `0c11d81a`, GPT-5.6 Sol was the sole sequential implementation writer. The builder did not commit, push, edit the PR, merge, deploy or activate a runner; the coordinator handles the user-authorized GitHub checkpoints. No new specification was written. The initial worktree was clean. The accepted specification remains `docs/design/slack-timeline-reliability.md` §§112–116.

`getCachedWorkTimeline` now reads all three `slack_team_state` generations through the indexed source-owned helper before any process-local or persisted hit. A failed generation read propagates. Cache payloads and memory entries carry all three text counters; legacy unstamped payloads miss. Identity and presentation mismatches rebuild the pure ledger inline and refuse old synopsis salvage. Data mismatches also rebuild inline: this deliberately deviates from the specified stale/background data-only path because the current legacy Slack reader has no sound current source-status and per-item reauthorization check for serving prior evidence. The request pays a full pure timeline query and enforcement resolution, plus before/after generation reads and a short publication transaction; no LLM runs inline. No latency measurement is available. Matching-generation TTL staleness retains the prior SWR behavior. Cache rebuilds use a strict Slack item/identity read option so a failed core Slack leg cannot be published as a fresh partial payload; direct legacy reader calls retain their current behavior. Payload version and synopsis floor both rise to 15 for changed authorship/day meaning, and salvage requires exact data, identity and presentation stamps.

Cold and background builds read generations before and after work, retry once if overtaken, and publish through a short transaction that creates/locks the team generation row, compares all three revisions under `FOR SHARE`, then upserts the stamped cache row. Memory is populated only after accepted publication. This closes the missing-row race and rejects an old build after a committed source revision. The no-revision-change revisit still permits a cache hit. The visibility variant key remains unchanged. Unit tests cover salvage incompatibility and a CAS refusal for each revision; a new real-Postgres suite covers memory/persisted generation reads, read failure, remap and no old synopsis, title/presentation invalidation, unchanged revisit, data inline rebuild, a second module instance as a two-worker memory simulation, overtaken inline build, and unstamped legacy row. The coordinator later ran the new generation, timeline cache, synopsis salvage, degraded-persistence and freshness-contract suites against isolated real Postgres: **26/26 passed** across five files.

Builder checks on this uncommitted snapshot: the final focused unit/guard command passed **49/49** across nine test files; `npm run typecheck`, `npm run check:docs` (67 routes, 97 tables, 8 sources), targeted ESLint (zero errors, one pre-existing unused-variable warning in `work-timeline.ts`) and `git diff --check` passed. `scripts/dm-isolated.sh` could not reach the Docker daemon socket (`operation not permitted`); local `initdb` also failed at bootstrap with `shmget: Operation not permitted`. The coordinator verified the focused real-Postgres set as recorded above. A task-owned ignored checkpoint is retained under `.context/aio-1170-handoff/cache-reader-checkpoint`.

Remaining gates: review same-hash membership-revocation handling and current source-status authorization before allowing any data-only stale serve; integrate the canonical ledger/identity reader and preserve strict failure semantics through API/UI; verify actual identity writers and Slack presentation changes advance the right generation; run required integrated Fable and fresh Astra reviews. The inactive Slack runner remains unreachable. This packet is a bounded cache-generation mechanism, not AIO-1170 completion or live incident certification.

Coordinator adjacent checks passed: access-enforced timeline, work-timeline and meeting real-Postgres suites **43/43** across three files, plus timeline HTTP, summary, generation, synopsis and payload-shape unit/HTTP suites **39/39** across seven files. The subsequent independent pre-push cache review found the blockers recorded below.

### Accepted cache-reader review fixes — September 19, uncommitted

The independent review confirmed two P1 defects in the active reader: an item-grain membership close can revoke a Slack title/synopsis without changing the project-set hash or any Slack generation, and `memberVisibility` discarded the oracle's read-error bit before cache lookup. GPT-5.6 Sol applied the accepted fixes as sole writer to the current uncommitted packet. No new spec, commit, push, PR, merge, deploy or runner activation was performed. The coordinator read Codex weekly utilization at 15%, below the user's 40% cap.

Each active cache read now resolves the current item set through the existing enforcement read primitive, hashes sorted item IDs, and compares that fingerprint with memory and persisted payload stamps. A missing or changed stamp misses. Cold and background builds compare item visibility before/after their pure build, and the publication transaction rechecks it against the stamped set before upsert. Later hits recheck live visibility, including a membership close that commits after publication. Synopsis salvage requires the same fingerprint; a revoked item cannot lend old prose to its replacement ledger. This adds a full current-membership read to each hit, so the old cheap project-hash-only hit cost no longer applies. The implementation does not add an ACL generation service.

`memberVisibility` now uses `visibleProjectsWithError` and throws on member/group/grant substrate read errors, while a genuinely grantless member remains an empty project set. Cold request builds use strict Slack reads on the expanded 8–30 day dashboard route too. A healthy authorized pure build can return an uncached degraded ledger when the cache payload write alone fails, after successful final generation and item-visibility rechecks; source, access, generation and uncertain-commit failures still propagate. Data-only Slack generation mismatches continue to rebuild inline as documented above, with the same performance/spec deviation and no measured latency.

Added real-Postgres coverage for same-hash reachable membership closes through `closeMembershipInto` on warm and second-worker persisted hits, a cold build race, a background rebuild race, oracle read failures, grantless member, and cache-only write failure. Added unit coverage for item-fingerprint publication/salvage gates and both dashboard window failure branches. Focused local verification passed: `npm run typecheck`, targeted ESLint, `git diff --check`, and **29/29** targeted unit/guard tests across five files. The coordinator updated two older real-Postgres fixtures for the deliberate changed authorization behavior, then passed **26/26** tests across four focused files in this worktree's isolated database. The independent final re-review found no new actionable issue in the packet; it confirmed the live identity writer remains a separate integration gate.

Reader generation fencing alone does not complete the identity writer contract: `bumpSlackIdentityGeneration` currently has no production caller. Existing identity remap paths still use best-effort local cache busts, so another worker can retain old credit until TTL. The remap test manually bumps the counter and proves only the reader mechanism. Integrating the live identity writer and proving cross-worker invalidation remain an AC-08 release gate.

### Slack identity-generation writer packet — September 19, uncommitted

This bounded continuation began at clean `ae95cb0b` on `codex/aio-1170-resume`, with the accepted spec unchanged. Inspection found that the shared `setMemberIdentity` / `removeMemberIdentity` functions are the explicit production `member_identities` mutation paths used by Admin link/unlink, Slack auto-sync, OAuth/token capture and `scripts/admin.ts`; the other provider syncs also use them. Member hard deletion and invite rollback additionally remove identity rows through the member FK cascade. No production path called `bumpSlackIdentityGeneration` before this packet. The per-process `reconcileAttribution` coalescer runs `reattributeItems` asynchronously and can modify Slack `items.member_id` and `item_versions.member_id` after the mapping write; its best-effort local timeline bust cannot invalidate a second worker's fresh memory entry.

The shared identity writer serializes mutations of each provider ID with a transaction-scoped advisory lock, reads the current mapping in that transaction, preserves the existing force/collision and best-effort audit behavior, and bumps Slack identity generation in the same transaction as an actual create, forced remap or remove. Hard member deletion and invite rollback lock the member row, check for Slack links, and commit the FK cascade with one identity bump if needed. Same-member handle/email updates, repeated auto-sync, rejected collisions, absent removes and Linear/Plane mutations do not bump. A failed generation write rolls the mapping mutation or member deletion back. Mapping reads now propagate errors instead of treating an unreadable row as absent.

The historical reattribution pass reads a strict identity map and fences it against the Slack identity revision. Each actual Slack item or version correction and its generation bump commit or roll back together. A concurrent remap invalidates an older scan's map snapshot; the scan retries once and checks the revision again at completion. Existing conservative no-unattribute behavior, admin item locks, external-row exclusion and non-Slack update behavior remain. Per-row bumps ensure a completed scan cannot leave a second worker's earlier pure rebuild and synopsis stamped as current after a later owner/version correction. The revision may advance more than once for a multi-row repair; that is intentional and only actual changed Slack rows advance it.

Real-Postgres cases were added for Slack create/remap/remove and hard-delete cascades versus metadata/no-op/other-provider writes; rollback of create, remap, remove and hard delete when the generation bump fails; failed Slack item and version correction rollback; repeated auto-sync; scans overtaken by remap before and during a write; warm and persisted second-worker cache invalidation through `setMemberIdentity`; and an in-flight cache build overtaken by the real identity writer. The prior cache remap fixture's manual SQL bump was replaced with the production writer. The coordinator ran the targeted real-Postgres suites in this worktree's isolated container: **30/30 passed** before the lifecycle review correction, then **33/33 passed** with the correction. Focused unit/guard tests passed **13/13** across four files; `npm run typecheck`, `npm run check:docs` (67 routes, 97 tables, 8 sources), targeted ESLint and `git diff --check` passed on the builder snapshot.

The independent review found a MEDIUM active-roster gap: soft disable and invited-member activation changed which Slack people render without invalidating other workers. Soft disable now advances the identity revision in the status-change transaction when an active member has a Slack link; both team-scoped activation paths advance it in the same SQL statement when an invited member with a Slack link becomes active. Repeated status calls do not bump. Two-worker tests verify disabled credit disappears and activated credit appears on the next read. The independent corrected-diff re-review found no actionable issue.

This packet closes the identified production identity-generation writer gap for active Slack identity and roster transitions. The broader accepted namespace/suppression migration, canonical ledger/reader cutover, historical repair and live acceptance remain pending. No new Slack runner was activated.

The coordinator launched the subscription-authenticated GPT-5.6 Sol CLI with approved local state access for this packet and read Codex weekly usage at **17%**, below the user's 40% cap.

### Account-suppression substrate packet — September 19, uncommitted

This bounded continuation started from clean, reviewed `d587d578` on `codex/aio-1170-resume`. The accepted specification in `slack-timeline-reliability.md` §Message ledger and canonical identity was unchanged. GPT-5.6 Sol was the sole implementation writer; the coordinator handles the user's requested GitHub checkpoints and staging PR. Codex weekly utilization was 19% on the coordinator's latest read, below the 40% cap. No deployment, merge or new Slack runner activation occurred.

`member_identity_suppressions` is now a team/provider/external-ID primary-keyed table with a replayable additive migration and schema mirror. It has no member FK, so deleting a member cannot lift an intentional unlink. The shared `setMemberIdentity` / `removeMemberIdentity` transaction and team advisory lock own all Slack suppression reads and writes. An explicit Slack unlink removes the mapping, inserts the suppression, and advances identity generation in one transaction. An unlink of an absent Slack key creates the fence with `removed:false` and one generation bump so a pending email sync cannot create the link afterward; repeating it is a true no-op. Non-Slack unlink behavior is unchanged. Automatic Slack email sync reaches the same writer without explicit authority, treats suppression as a conflict/skip, and never clears it. Admin link, CLI link, and authenticated OAuth/pasted-token capture pass explicit authority; a successful relink clears the exact suppression in the mapping transaction and advances identity generation. A collision returns without clearing suppression or bumping. Credential save still succeeds independently, but OAuth HTML and pasted-token JSON now report identity pending/conflict rather than imply attribution succeeded. Failed suppression reads/writes abort the transaction and roll back mapping changes. The writer checks target team membership before a link.

Before the attended namespace cutover, the writer refuses to create a second live raw/qualified row for one observed Slack user ID. An unlink by the alternate key shape reports the existing live key instead of silently creating a fence for the wrong key. A raw suppression conservatively blocks creation of a new qualified row by automatic sync; an authorized qualified relink can proceed after the raw row is removed, and its subsequent metadata sync can update the established qualified row. The raw suppression stays in place until the attended provenance migration. This packet does not migrate raw rows, enforce the future cutover marker, switch the active resolver, or activate a Slack source runner.

Five new real-Postgres tests cover unlink → automatic sync refusal → authorized relink, collisions and repeated/absent no-ops, rollback and fail-closed reads on injected suppression faults, both sync/unlink lock orderings, raw/qualified duplication prevention, and migration replay with live mapping and suppression data. The existing pasted-token suite also gains a credential-success/identity-conflict case. The coordinator recreated this worktree's isolated Postgres container with the new schema: the suppression, Slack identity and timeline-cache suites passed **28/28** before the review correction. The adjacent pasted-token, OAuth and member-identity suites passed **14/14**. On the builder snapshot, focused unit/guard tests passed **9/9** across two files; `npm run typecheck`, targeted ESLint with no warnings, `npm run check:docs` (67 routes, 98 tables, 8 sources), and `git diff --check` passed. The docs inventory now lists the new table. An ignored local tar checkpoint of source, migration, tests and records is under `.context/aio-1170-handoff/account-suppression`; it is a local recovery copy.

Broader AIO-1170 gates remain the attended identity namespace migration and consumer cutover, historical repair, integrated reviews and live acceptance. No Slack runner was activated.

### Account-suppression review corrections — September 19, uncommitted

Independent review identified a confirmed HIGH case-folding bypass in the Slack identity writer and an Admin action success response on a raw/qualified conflict. The sole implementation writer corrected these in the current uncommitted packet, without changing the accepted spec or activating a runner. Under the existing team advisory lock, Slack mapping and suppression reads now compare folded IDs, including previously stored lowercase keys. Mutations use the matched row ID or original suppression key. Raw/qualified counterpart checks fold the user ID while retaining the workspace namespace; a qualified ID in a different workspace remains distinct. An explicit relink clears every matching stored suppression spelling after a successful mapping write, while the existing raw-versus-qualified suppression rule remains in force. Multiple pre-existing live rows with the same folded key fail closed for link/unlink. The Admin link action returns `{ok:false,error:note}` on `SetIdentityResult.conflict` before revalidation or repair scheduling.

Added real-Postgres cases for lowercase raw and qualified link/unlink followed by uppercase auto-sync refusal and explicit relink, plus mixed-case live duplicate prevention, raw/qualified conflict and distinct-workspace behavior. The coordinator reran the corrected suppression and adjacent identity/OAuth suites in isolated Postgres: **45/45 passed** across six files. A new Admin action unit test covers the conflict response and successful-link path. Focused local unit/guard tests passed **67/67** across five files; `npm run typecheck`, targeted ESLint (zero warnings), `npm run check:docs` (67 routes, 98 tables, 8 sources), and `git diff --check` passed. The independent post-fix reviewer found no remaining actionable issue.

### Inactive canonical Slack message read packet — September 19, uncommitted

GPT-5.6 Sol continued as sole implementation writer from clean reviewed `f1b3d44c` on `codex/aio-1170-resume`. The accepted specification was read and unchanged. Codex weekly usage was reported at 19%, below the user's 40% cap. No commit, push, PR edit, merge, deployment or source-runner activation was performed by the builder.

`lib/ingest/slack-message-read.ts` is a source-owned, currently unimported read helper. It accepts a team, fixed inclusive UTC rolling window and the current access oracle's authorized item IDs. SQL applies team, item visibility, eligibility, deletion and time predicates before each page limit. It exhausts the source ledger through a total `(occurred_at,item_id,message_ts,workspace_id,channel_id)` keyset, returning exact microsecond timestamps, raw source author, item/workspace/channel/root/message IDs and root status. All pages run on one connection in a repeatable-read, read-only transaction, so concurrent atomic publication cannot produce a mixed committed state. There is no total-row cap; a failed page rejects the whole call. It does not resolve identities, infer days, grant access or switch the active timeline reader. The later caller must still reauthorize visible item IDs before use. The per-page bound is 512 rows and callers may choose a smaller page for tests.

New real-Postgres cases cover more than `ITEM_LIMIT` old visible and in-window invisible rows around an old thread's recent reply, 46 same-day rows over multiple small pages with equal instants, inclusive window edges, deletion/exclusion and another team, and an injected SQL failure after a real first page. The independent review then found two issues: the old keyset could collide across workspace/channel scopes, and separate READ COMMITTED pages could mix pre-publication and post-publication rows. The correction adds a `pageSize=1` collision case with the same item, instant and message timestamp in four scopes, plus a deterministic between-pages atomic insertion case that checks the completed result against one snapshot and verifies both new rows committed. The coordinator ran the corrected reader and adjacent ledger suites in isolated Postgres: **16/16 passed** across two files. The independent corrected-diff re-review found no actionable residual. Focused source-evidence and inactive-wiring unit/guard suites passed **46/46**; `npm run typecheck`, targeted ESLint with zero warnings, `npm run check:docs` (67 routes, 98 tables, 8 sources), `git diff --check`, and separate untracked-file whitespace checks passed. The active timeline and source runner remain unchanged; canonical identity and timeline cutover are still separate gates.

### Inactive message-to-person-day projection packet — September 19, uncommitted

From clean reviewed `b665b032` on `codex/aio-1170-resume`, GPT-5.6 Sol continued as the sole implementation writer for this bounded packet. The accepted specification was read and not revised. The user's reported weekly Codex usage was 20%, below the 40% cap. No commit, push, PR edit, merge, deployment or Slack runner activation was performed by this writer.

`lib/ingest/slack-person-day.ts` now projects a complete `VisibleSlackMessage[]` into stable `(canonical item, member, UTC day)` evidence. Its only identity input is an injected resolver called with the exact `workspace:author` ID. A null result produces no member row; resolver failures propagate. The helper never reads a raw-ID map or falls back to item owner or versions. It rejects conflicting item/thread bindings or duplicate source-message identities, folds multiple messages and multiple linked accounts for one member on a thread/day, preserves ordered source message IDs and exact microsecond instants, and marks root authorship only for a surviving root actually authored by that member on that day. It does not create rows for absent dates. Output retains item, workspace, channel and root identity for a later authorized source link and lock-aware timeline projection. No active timeline code imports it.

The pure suite covers UTC midnight and neighboring microseconds, repeated messages and two accounts on one day, sparse replies to an old thread with an unmapped root, same-day root/replier separation, root removal, ambiguous/unmapped identity, resolver failure, input-order determinism, duplicate input and conflicting source identity. The real-Postgres-through-reader additions exercise an old item with recent replies across three UTC days and two accounts, exact microseconds and a small reader page size, plus a mapped root deletion that removes root credit while retaining a live mapped reply and excluding an ambiguous reply. The builder's sandbox could not reach the Docker socket, so the coordinator ran the focused file through the authorized isolated PostgreSQL harness. All **8/8 real-Postgres cases passed**. The independent reviewer found no actionable correctness issue in the projection or its reader-boundary tests.

Final local checks at 16:33 EDT: `npx vitest run test/slack-person-day.test.ts` passed **5/5**; `npm run typecheck`, targeted ESLint on the new module and two test files, `npm run check:docs` (67 routes, 98 tables, 8 sources), and `git diff --check` passed. Separate whitespace inspection of the new files found no trailing whitespace. Cutover gate: the timeline caller must supply the current canonical workspace-qualified identity resolver, reauthorize source item visibility after the ledger read, and apply the shared correction-lock credit oracle. Canonical identity ambiguity must resolve to no member, never to a raw alias or item owner. This packet does not satisfy the broader AC-02/04/05/08 integration and live acceptance gates.

### Active Timeline UI snapshot replacement packet — September 19, uncommitted

GPT-5.6 Sol was the sole implementation writer from clean `9578241a` on `codex/aio-1170-resume`, following the accepted Query/cache/API/UI design without a spec change. The user reported Codex weekly usage at 20% at packet start, below the 40% cap. No commit, push, PR or Linear edit, merge, deployment, or Slack runner activation was performed by this writer.

`TimelinePanel` now gives its SSR days to one keyed client owner rather than rendering a fixed `TimelineDays` sibling. `TimelineLoadMore` renders those initial days and replaces the entire visible snapshot after a successful expanded API response, including revised evidence on dates already shown. The state reducer preserves existing days during a request and on failure, handles empty initial days and rejects stale request actions. The fetch path blocks duplicate clicks before the next render, aborts on unmount, ignores stale resolution, validates that the response includes a days array, and advances the requested window only on success. The existing dashboard API URL/auth contract and seven-day step/cap remain unchanged. No cursor or Slack continuation control was added; backend continuation and the accepted shared evidence merge remain later integration work.

Focused tests cover one initial render with an expansion control, empty initial expansion, full same-date replacement, retained data on failure, stale result rejection, and an empty-to-older transition. Final local verification at 16:39 EDT: `npx vitest run test/timeline-expansion.test.ts test/timeline-panel.test.ts test/dashboard-timeline-route.test.ts` passed **7/7**; `npm run typecheck`, targeted ESLint with zero warnings, `npm run check:docs` (67 routes, 98 tables, 8 sources), and `git diff --check` passed. `npm run lint` exited successfully with 22 warnings in untouched repository and ignored handoff files; the changed source and tests have zero warnings. An initial targeted lint run found one unused test import; it was removed before the final passing run. New untracked files passed a separate trailing-whitespace scan. No browser interaction or live Slack acceptance was run for this UI-only packet. Broader AIO-1170 integration, review and live acceptance gates remain pending.

### Timeline UI P1 refresh privacy correction — September 19, uncommitted

Independent review and an adversarial check confirmed that the client owner retained reducer days across a same-viewer `router.refresh()` because its key contained only team/tier/member. A refreshed server snapshot could remove a revoked, privatized or deleted title while the mounted client still rendered it. A days-only fingerprint also missed revoked evidence loaded through an older window when the seven-day SSR snapshot stayed identical; a mounted React regression reproduced that residual case.

`TimelinePanel` now computes a fixed-length SHA-256 owner key from the viewer scope, authorized seven-day payload and a fresh server-render UUID. This makes every new server render replace the client owner synchronously, including when older expanded evidence changes without altering the seven-day payload. `TimelineLoadMore`'s existing unmount cleanup aborts the prior expansion; its request-ID and abort checks keep a late response from updating the new owner. The tradeoff is that a route refresh resets the expanded window even when the visible data did not change. No new authority service, cache contract or API route was added.

`test/timeline-panel-refresh.test.ts` uses React's client reconciler on a minimal in-process DOM host. It updates the same root and same viewer with a new authorized snapshot, checks immediate removal of private text, checks abort and late-response isolation, and checks that an identical seven-day snapshot still clears revoked older expansion. With the old fixed key restored temporarily, the first two cases failed; with a days-only hash, the older-expansion case failed. The corrected run passed **3/3**.

Final local verification at 16:46 EDT: `npx vitest run test/timeline-expansion.test.ts test/timeline-panel.test.ts test/timeline-panel-refresh.test.ts test/dashboard-timeline-route.test.ts` passed **10/10** across four files. `npm run typecheck`, targeted ESLint on changed source and tests with zero warnings, `npm run check:docs` (67 routes, 98 tables, 8 sources), `git diff --check`, and a separate trailing-whitespace scan of untracked files all passed. The task-owned uncommitted packet has a local ignored tar backup at `.context/aio-1170-handoff/timeline-ui-p1-checkpoint.tgz` with its SHA-256 in the adjacent `.sha256` file. No browser/live Slack acceptance was run; broader AIO-1170 integration and review gates remain pending. No commit, push, PR/Linear edit, merge or deployment was performed by the sole writer.

### Timeline Slack SourceGroup retention packet — September 19, uncommitted

GPT-5.6 Sol was the sole implementation writer from clean `920411b2` on `codex/aio-1170-resume`. The accepted specification remained unchanged; the user's reported weekly Codex usage was 21% at packet start, below the 40% cap. This packet changes only pure timeline grouping, its unit tests and this record. No commit, push, PR/Linear edit, merge, deployment or Slack runner activation was performed by the writer.

`toSourceGroups` now retains every loaded Slack evidence item in task-associated and Other source groups, including when `groupTimeline` receives an explicit `perSourceCap`. Slack items remain newest-first with evidence ID breaking equal-time ties. Non-Slack groups retain the existing per-source cap and ordering; signals keep their cap. The separate summary prompt item cap remains in effect. `SourceGroup.count` and person/task totals continue to count all loaded work, and the existing card maps every retained `group.items` row.

Three focused cases cover more than six linked and unlinked Slack items mixed with capped GitHub/Notion evidence, an explicit cap with capped signals and summary input, and equal-time input-order independence. The first two cases failed against the previous six-row/explicit slicing behavior. Final local verification at 16:50 EDT: `npx vitest run test/timeline-group.test.ts test/timeline-summary.test.ts` passed **28/28** across two files; `npm run typecheck`, targeted ESLint with zero warnings, `npm run check:docs` (67 routes, 98 tables, 8 sources), and `git diff --check` passed. These pure tests do not exercise a live ledger or database. The canonical Slack message reader and person-day projector are still inactive helpers; no active ledger-to-timeline integration, continuation API, identity cutover or UI continuation control was added here. Broader AIO-1170 integration, review and live acceptance remain pending.

### Pure Timeline Slack continuation merge packet — September 19, uncommitted

GPT-5.6 Sol was the sole implementation writer from clean `bd2cbad7` on `codex/aio-1170-resume`, using the accepted Query/cache/API/UI design without a new specification. The user's reported weekly Codex usage was 22% at packet start, below the 40% cap. This packet adds only `lib/dashboard/timeline-continuation-merge.ts`, its focused unit suite, and this record. The module is pure and client-safe; no active Timeline, API, v1 or cache caller imports it. No commit, push, PR/Linear edit, merge, deployment or Slack runner activation was performed by the writer.

`mergeTimelineSlackContinuation(existing, page)` accepts the current `TimelineDay[]` hierarchy and one Slack-only page. It unions rows by date, member, task or Other, Slack source and `EvidenceItem.id`. Replayed rows do not change counts or remove a valid synopsis; a new association or evidence row clears that person's synopsis. Each Slack source count is rebuilt from unique loaded IDs, each task count from its source groups, and the person's work total counts each Slack evidence ID once across task associations while retaining opaque capped non-Slack counts. Non-Slack rows/counts and signal groups remain present. Days, people, tasks, source groups and items receive deterministic ordering. Empty `[]` pages are valid. Invalid structures, non-Slack page groups, wrong UTC day, conflicting repeated evidence or metadata, and initial Slack groups whose count differs from their visible rows are rejected. The caller remains responsible for generation, asOf, visibility and cursor compatibility; this module does not claim to authorize either input.

The current DTO has only `EvidenceItem.id` for the source evidence key. A future continuation producer must assign a stable ID at the intended contribution evidence grain; the active legacy Timeline uses a thread/participant-derived ID and is unchanged here. The DTO also has no distinct unique-person-total versus association-row-count field: this helper sets `PersonDay.total` to unique Slack credit and leaves each task/source count as rows shown under that association. The current `personSummary` card text recomputes its own item count by summing task/source counts, so a later UI integration must use the unique total or explicitly label association counts. Capped non-Slack rows cannot be deduplicated beyond their displayed items, so their existing counts are preserved as opaque totals. No endpoint, cursor, UI control, v1 drain, canonical identity cutover or source runner was added in this packet.

Focused unit cases cover same-date and same-person merge, shared Slack evidence under two tasks without double person credit, page order/replay/empty-page determinism, capped non-Slack counts and signals, synopsis invalidation, and malformed/non-Slack/conflicting page refusal. Local verification: `npx vitest run test/timeline-continuation-merge.test.ts test/timeline-group.test.ts` passed **25/25** across two files; `npm run typecheck`, targeted ESLint with zero warnings, `npm run check:docs` (67 routes, 98 tables, 8 sources), `git diff --check` and a separate untracked-file whitespace scan passed. These pure tests do not exercise database, HTTP, browser or live Slack behavior. Integrated review and live acceptance remain pending.

Independent pre-push review found no HIGH/blocker. It noted one LOW inactive-input edge: the helper currently permits the meeting-only `via:"submitter"` marker on a Slack row, so two task associations for one evidence ID could disagree on that marker. No active caller produces this shape; require the future Slack page producer to omit it or reject it during integration. The reviewer also confirmed that task-specific `linkVia` may legitimately differ across associations and that stable source-evidence IDs are required before wiring.

### Inactive Slack identity cutover database guard — September 19, uncommitted

GPT-5.6 Sol was the sole implementation writer from clean `efbc324d` on `codex/aio-1170-resume`. The accepted §Message ledger and canonical identity specification was unchanged; weekly Codex usage was reported at 22% at packet start, below the user's 40% cap. The coordinator owns GitHub checkpoints. This packet adds only a schema mirror, replayable migration and real-Postgres test file. It does not change identity application writers/readers, set a marker, migrate/archive a row, activate the Slack runner, or deploy.

`teams.slack_identity_cutover_at` is nullable and defaults to NULL. After a later attended transaction updates a team's rows and sets this column, the `member_identities` INSERT/UPDATE trigger requires Slack external IDs to be exact uppercase ASCII-alphanumeric `WORKSPACE:USER` values; it rejects raw IDs and metadata updates on surviving raw rows with a static, non-identifying SQLSTATE `23514` message. It leaves DELETE, other providers and unmarked teams alone. The trigger locks the team row `FOR SHARE` before checking the marker, which conflicts with the marker UPDATE and prevents an old worker's raw write from committing after the marker transaction. A team trigger prevents clearing or changing a committed marker while preserving ordinary team updates and team deletion. Schema and migration create triggers only when missing and replace their functions in place, so replay does not remove an active fence. The attendee must still disable/drain legacy writers, reconcile all raw rows and set the marker last in the same transaction; this substrate cannot prove provenance or repair a raw row already committed before marking.

The new real-Postgres suite covers unmarked raw inserts/updates, marked raw insert and update refusal, qualified acceptance and malformed/case variants, team/provider isolation, an observed concurrent old-worker insert blocked behind the marker transaction, same-transaction conversion and rollback, unrestricted raw deletion, marker durability and two migration replays. The builder sandbox could not reach Docker (exit 126), so the coordinator ran the authorized isolated PostgreSQL harness with a recreated schema: **7/7 real-Postgres tests passed**. The two focused migration/schema guards passed **6/6**. `npm run typecheck`, `npm run check:docs` (67 routes, 98 tables, 8 sources), targeted ESLint and `git diff --check` passed locally. Full `npm run lint` exited 0 with 22 warnings in untouched repository and ignored handoff files; the changed test has zero warnings. No integrated Fable/Astra review or live cutover is claimed.

Independent review and adversarial recheck found one HIGH bypass in this uncommitted packet: `member_identities.provider` has no canonical-value constraint, but the trigger returned before locking the team unless `NEW.provider` was exactly `slack`. A marked team could therefore accept a raw `external_id` under `Slack` or ` slack `. The shared resolver trims and lowercases provider keys, so it could credit such a row as Slack while the timeline's direct `provider = 'slack'` map omitted it. The first correction classified Slack with `lower(btrim(NEW.provider)) = 'slack'`, locked and inspected the marker, and after activation required both exact stored provider `slack` and an uppercase ASCII `WORKSPACE:USER` external ID. Refusal stays static and non-identifying; other providers and unmarked teams keep their existing behavior. Focused real-Postgres cases cover provider variants on raw INSERT, conversion UPDATE from another provider, qualified variants after the marker, and a mixed-case writer blocked behind a concurrent marker transaction. The builder's fresh-schema isolated PostgreSQL attempt failed before startup because the sandbox denied Docker socket access (exit 126). The earlier 7/7 result does not attest that correction. After the first fix, local typecheck, docs drift, targeted ESLint and both focused migration/schema guards (6/6) passed.

A second independent HIGH review and skeptical check found that PostgreSQL's one-argument `btrim` removes only U+0020, while the shared JavaScript resolver uses `String.trim()`. A marked team could still receive a raw identity under provider NBSP+`Slack`+NBSP, tab/newline, or FEFF; the guard would skip it and the resolver would credit it as Slack. Schema and migration now share an `is_slack_identity_provider(text)` SQL function that trims the complete ECMAScript whitespace/line-terminator set before lowercasing and comparing to `slack`. The trigger uses that classifier before taking the team lock. After the marker it still accepts only exact stored `slack` and uppercase ASCII `WORKSPACE:USER`; pre-marker writes, unrelated providers and other teams retain their behavior. The real-Postgres suite checks raw and qualified variants for every listed trim character, legacy metadata UPDATE, conversion from another provider, isolation, and a non-trimmed U+200B control. A pure shared-resolver case checks NBSP, tab/newline and FEFF normalization.

Second-fix local verification: pure resolver and repository guard tests passed **14/14** across three files; `npm run typecheck`, targeted ESLint with zero warnings, `npm run check:docs` (67 routes, 98 tables, 8 sources), `git diff --check`, untracked-file whitespace scan and a direct schema/migration guard-block comparison passed. Node enumerated the BMP characters removed by `String.trim()` and confirmed an exact match with the 25-code-point SQL list. The builder could not reach Docker, so the coordinator recreated the authorized isolated PostgreSQL schema and ran the corrected guard suite: **9/9 real-Postgres cases passed**. Earlier 7/7 and 8/8 PostgreSQL results predated the final correction; this 9/9 run verifies the current snapshot.

Attended cutover lock order: take `pg_advisory_xact_lock(7341014, hashtext(team_id))` before scanning identities or updating the team marker, matching the shared identity writer's team lock. Then reconcile every legacy row where `is_slack_identity_provider(provider)` is true, including already-qualified IDs stored under noncanonical provider text, before setting the marker last in the same transaction. The trigger fences future writes but does not scan or repair untouched rows. This ordering avoids a marker transaction taking the team row lock first and then waiting on an identity row held by a writer that is itself waiting for the team row lock.

### Inactive legacy Slack identity provenance classifier — September 19, uncommitted

GPT-5.6 Sol was the sole implementation writer from clean `18670c24` on `codex/aio-1170-resume`. The accepted specification was reused without a new spec. The user reported 24% Codex weekly usage at packet start under the 40% cap; no fresh utilization reading was available to this writer. The coordinator owns GitHub checkpoints. This packet adds only `lib/identity/slack-cutover-provenance.ts`, its focused unit suite, and this record. No commit, push, PR/Linear edit, merge, deployment, marker write, database migration or Slack runner activation was performed by this writer.

`classifySlackIdentityCutover` is a synchronous pure planner over explicit team-scoped Slack identity rows and future verified historical source/account observations. It validates the same team ID on the rows, census and each inventory. A proposed `qualify_in_place` carries the original row ID and member ID and appears only when a completed evidence-referenced historical source census and each workspace's completed account inventory establish exactly one workspace/account whose verified member matches the raw row. The function cannot itself prove the future discovery operator's census is exhaustive or that writers are paused; that step must retain the referenced evidence from durable integration/auth history, including every provider spelling variant, and verify each account/member from auth/link history rather than email or handles. A missing census, empty census, or missing workspace inventory cannot yield a qualification; already observed multiple workspaces can still be reported as ambiguous. A current integration, one qualified mapping, frontmatter or display data does not close the world.

Other sanitized outcomes distinguish unknown account, two-workspace ambiguity even for the same member, member mismatch, same-member and conflicting qualified-row collisions, duplicate raw/qualified rows, noncanonical provider/qualified rows, and qualified-row status. Both collision kinds require manual review; there is no automatic reconciliation, archive, marker, DB access or mutating caller. Invalid IDs, mixed teams/providers and conflicting duplicate observations throw static errors before any plan is returned. Row order, source order, inventory order and account order do not change the plan. Output includes only allowlisted row/member/account IDs and status, never email or token fields.

The first focused run was 7/8: a test incorrectly treated `U:123` as malformed when it is a valid qualified ID. That fixture was corrected to `U:12:3`; the classifier also now reports observed two-workspace ambiguity before pending coverage. The subsequent focused run passed 9/9 before noncanonical-row hardening. After final team-ID validation, the focused suite passed **10/10**; `npm run typecheck`, targeted ESLint with zero warnings, `npm run check:docs` (67 routes, 98 tables, 8 sources), `git diff --check` and an untracked-file whitespace scan passed. Full `npm run lint` exited 0 with 22 warnings in untouched repository/ignored handoff files before the final team-ID addition; targeted lint passed on final source and tests. This pure packet does not discover provenance, reconcile rows, attest account enumeration, lock writers, set a cutover marker or prove live migration safety. Integrated Fable/Astra review and live acceptance remain pending.

Independent code review then confirmed a HIGH observation-contract defect: requiring a member ID for every account made an observed but unmapped `U123` in a second workspace unrepresentable. An adapter could omit it and the raw row could be falsely proposed for qualification from the first workspace. The contract now permits `memberId: null` for observed account existence without a verified member mapping. Every observed user ID participates in workspace ambiguity; a sole unmapped account stays unresolved. Duplicate observations with different mappings, including mapped versus unmapped in either order, fail closed with a static error.

The review also found a MEDIUM qualified-row defect: canonical spelling alone previously yielded `qualified_noop` despite a missing census, missing inventory, absent account, unmapped account, conflicting member, or a workspace absent from the census. The classifier now emits separate sanitized statuses for those cases. A verified no-op requires a complete historical census, every enumerated workspace inventory, the observed account, and a matching verified member. Known mapping conflicts remain visible even while other provenance is incomplete. These statuses request operator review; the pure helper does not alter an explicit manual link based on an observation. The future discovery adapter must still certify complete provider account enumeration, including unmapped accounts; this pure function cannot independently prove that certification.

Corrected snapshot checks on September 19: `npx vitest run test/slack-cutover-provenance.test.ts` passed **15/15**; `npm run typecheck`, targeted ESLint on the classifier and test with zero warnings, `npm run check:docs` (67 routes, 98 tables, 8 sources), `git diff --check`, and a separate trailing-whitespace scan of both untracked TypeScript files passed. The existing independent HIGH and MEDIUM findings are corrected in this uncommitted packet; no post-fix independent review is claimed. The helper remains inactive and the accepted specification is unchanged.

A second independent HIGH review, confirmed by a skeptic, found that the classifier could still propose `U123` → `T1:U123` when an existing qualified `T2:U123` row contradicted a T1-only historical census. A different `T2:U999` qualified row outside that census likewise invalidated the claimed complete workspace universe for every raw row. An account absent from a purportedly complete T2 inventory had the same global effect. The second correction inspects every existing qualified Slack row before producing any raw qualification. A qualified workspace outside the census; an account absent from an available completed inventory; an unmapped or mismatched member; or duplicate/noncanonical qualified rows now block all `qualify_in_place` proposals. The last two classes are conservatively global cutover blockers even when the account universe itself may be correct. The specific qualified row keeps its sanitized review status; otherwise matching raw rows report `pending_conflicting_provenance`, and other qualified rows report `qualified_pending_conflicting_provenance`. Observed two-workspace raw accounts remain ambiguous. A qualified row is never used as proof of a workspace account or member mapping.

Second-correction focused fixtures cover the same-user and different-user T2 rows outside a T1-only census, an omitted qualified account from a complete T2 inventory, member/spelling/duplicate conflicts, and input-order plus identical-row replay. Final local verification on this uncommitted snapshot passed: `npx vitest run test/slack-cutover-provenance.test.ts` **19/19**; `npm run typecheck`; targeted ESLint on both TypeScript files with zero warnings; `npm run check:docs` (67 routes, 98 tables, 8 sources); `git diff --check`; and a separate untracked-file whitespace scan. The pure classifier still depends on the future discovery adapter's honest, complete historical census and account inventories; it does not lock writers, reconcile identities or authorize cutover. No post-second-correction independent review or live acceptance is claimed. The user reported Codex weekly usage near 25% under the 40% cap; no fresh utilization reading was available to this writer.

### Inactive Slack identity cutover row snapshot reader — September 19, uncommitted

GPT-5.6 Sol was the sole implementation writer from clean `b8f90034` on `codex/aio-1170-resume`, reusing the accepted identity cutover specification without a new spec. The user's most recent weekly Codex report was near 25%, below the 40% cap. The coordinator owns GitHub checkpoints. This packet adds only `lib/identity/slack-cutover-rows.ts`, its real-Postgres data-mechanics suite and this record. The reader has no active caller, cache or HTTP route. No commit, push, PR/Linear edit, merge, deployment, marker write, migration or runner activation was performed by the writer.

`readSlackCutoverIdentityRows` reads every existing `member_identities` row for one UUID team where the database's `is_slack_identity_provider(provider)` recognizes the stored provider, including the complete ECMAScript trim set. It returns only row ID, team ID, member ID, exact stored provider and exact stored external ID. It uses one `REPEATABLE READ, READ ONLY` PostgreSQL transaction, a unique row-ID keyset and bounded pages without a total-row cap. Invalid team IDs or page sizes and any failed page reject the entire call. The real-Postgres cases cover team/provider isolation; raw, qualified and noncanonical provider/external-ID values; all Unicode trim variants; more than 512 rows and tied external IDs; concurrent insert/update/delete after the first page; failed between-page SQL refusal; and transaction read-only enforcement. The builder's isolated harness attempt exited 126 before tests because sandbox access to the Docker socket was denied. The coordinator then recreated the isolated PostgreSQL database and ran the exact new suite: **4/4 passed** at 17:39 ET on September 19. Other local checks on this snapshot: `npx vitest run test/slack-cutover-provenance.test.ts` passed **19/19**; `npm run typecheck`, targeted ESLint with zero warnings, `npm run check:docs` (67 routes, 98 tables, 8 sources), `git diff --check` and an untracked-file whitespace scan passed.

This snapshot supplies only the classifier's future `rows` input. It does not establish historical source/account provenance, authorize cutover, lock writers or validate a marker transaction. An attended migration must disable and drain old writers, acquire `pg_advisory_xact_lock(7341014, hashtext(team_id))`, then reread and validate all team Slack identities in its own transaction before setting the marker last. Rows can change after this read; its result must never be reused as migration authority.

### Slack auth.test workspace observations — September 19, uncommitted packet

GPT-5.6 Sol was the sole implementation writer from clean `ed380472` on `codex/aio-1170-resume`, continuing the accepted specification without a new spec. The user reported weekly Codex use near 25% against a 40% cap; this writer has no fresher utilization readout. The coordinator owns GitHub checkpoints. This packet adds a replayable additive migration and schema mirror for `slack_workspace_observations`, plus the existing binding writer's insert at a matched `recordSlackWorkspaceIdentity` update. The row stores only a DB-generated ID, team ID, integration UUID, exact workspace ID, first-observed timestamp and `auth_test` provenance. The integration UUID has no FK so a later integration deletion retains the observation. A repeat accepted auth.test is `ON CONFLICT DO NOTHING`. The insert uses the binding writer's caller transaction; an unmatched fence inserts nothing, and an insert failure rejects that transaction. There is no backfill from current bindings: earlier workspace rotations and deletions remain unknown, and this table cannot certify a complete historical census.

Eight new real-Postgres cases cover real discovery acceptance, same-workspace replay with stable ID/time, second-workspace rotation retaining the first, stale ref refusal, injected observation-insert failure rolling back the binding update, caller rollback removing both, team isolation, and integration deletion retaining non-secret evidence. **These cases have not run in this builder sandbox**: `npm run test:datamechanics:iso -- test/datamechanics/slack-workspace-observations.datamechanics.test.ts` exited 126 before test execution because access to the Docker socket was denied. The coordinator must run the isolated suite on a fresh schema, including the source-bootstrap regression path. Local checks on this packet: `npx vitest run test/slack-source-discovery.test.ts test/guards/slack-source-not-wired.test.ts` passed **18/18**; `npm run typecheck`, targeted ESLint on the changed TypeScript files with zero warnings, `npm run check:docs` (67 routes, 99 tables, 8 sources), and `git diff --check` passed. No runner, cutover marker, PR/Linear edit, commit, push, merge or deployment was performed by this writer. The broader historical provenance and live acceptance gates remain open.

Coordinator verification: the first fresh isolated PostgreSQL run passed **7/8**; the failing test expected `first_observed_at` to be a JavaScript `Date`, while the raw SQL helper returns timestamp text. After changing only that assertion to check a parseable timestamp string, the exact suite passed **8/8** on a freshly recreated isolated database at 17:46 ET. A broader fresh run of that suite plus the existing source-bootstrap suite passed **28/28** across two files at 17:47 ET. This result preceded independent pre-push review.

Independent pre-push review then found a **MEDIUM** integrity gap: the table was called append-only, but direct SQL could update or delete a recorded `auth.test` observation. GPT-5.6 Sol added the same replay-safe `protect_slack_workspace_observation()` function and guarded trigger to the migration and schema. Every UPDATE, including a no-op, raises static SQLSTATE `23514` with `Slack workspace observation is immutable`. DELETE raises the same error while the parent team exists. The existing `ON DELETE CASCADE` may delete observations after that team row is gone; the guard checks parent existence rather than generic trigger depth, so an unrelated nested trigger cannot use nesting alone to erase evidence. Integration deletion still retains observations because their integration UUID has no FK. The function emits no token, observation ID, workspace ID or team ID.

The new real-Postgres cases exercise direct UPDATE/DELETE refusal, no-op UPDATE, a DELETE invoked by an unrelated trigger, migration replay with one guard and stable observation identity/time, and team deletion cascade with another team's evidence retained. They bring this suite to **11 cases**, but **have not run on this corrected snapshot**: the exact isolated command again exited 126 before tests because Docker socket access was denied in the builder sandbox. The earlier **28/28** result predates this guard and does not attest its cascade behavior. On the corrected snapshot, `npm run typecheck`, targeted ESLint with zero warnings, `npm run check:docs` (67 routes, 99 tables, 8 sources), `git diff --check` plus untracked-file whitespace scan, exact guard-text mirror comparison, and the focused Vitest suites (**18/18**) passed. The coordinator must rerun the 11-case observation suite and the source-bootstrap regression suite against fresh isolated PostgreSQL before checkpoint or publication. No commit, push, PR/Linear edit, merge, deployment, runner activation or cutover was performed by this writer.

Coordinator verification after the guard: the observation and existing source-bootstrap suites passed **31/31** across two files on a freshly recreated isolated PostgreSQL database at 17:53 ET. This run covers the guard's direct/nested mutation refusals, replay and team cascade; the builder's earlier Docker denial remains a builder-environment limit only. Independent re-review of the corrected diff is pending.

Independent re-review found a remaining **MEDIUM** limit: PostgreSQL row triggers do not intercept privileged `TRUNCATE` or DDL. The shared data-mechanics setup intentionally truncates these tables between tests, and the schema-owning role could disable a new trigger as well. The requested statement-level trigger attempt was stopped before any change was made. Schema and migration now state the supported guarantee precisely: the application writer inserts only, ordinary row UPDATE/DELETE are guarded, and privileged table operations are outside this guarantee. Treat this as corroborating evidence, never as an exhaustive or tamper-proof census. A future cutover must resolve privileged database role separation or another integrity proof before relying on these observations alone. Defer that role-level hardening to the attended cutover packet; it is not part of this inactive source writer.

Final independent pre-push re-review found no HIGH/blocker or new actionable issue and accepted that bounded MEDIUM deferral. The observation packet is ready for a recovery checkpoint, not for cutover activation.

### Inactive shared Slack account lookup — September 19, uncommitted packet

GPT-5.6 Sol was the sole implementation writer from clean `d0fd8326` on `codex/aio-1170-resume`, continuing the accepted AIO-1170 specification. The user reported Codex weekly usage near 27%, below the 40% ceiling; this writer has no fresher authoritative utilization reading. The coordinator owns commits and GitHub checkpoints. This packet adds the pure `lookupSlackAccount` helper in `lib/identity/resolve.ts` and focused unit tests. It has no active caller and changes no timeline, credit, login, auto-sync, cutover marker, runner or database write.

The caller supplies a team-scoped live mapping snapshot, a verified item workspace when available, and, for workspace-free legacy IDs, an evidence-referenced historical source census, one complete account inventory for every source workspace, and a reviewed unresolved/quarantined legacy evidence disposition. Exact qualified lookups use only one canonical live `slack` `WORKSPACE:USER` row. Legacy lookup additionally requires the raw user in exactly one observed workspace, the observed account member to equal the sole canonical qualified mapping, no unresolved/quarantined evidence for that user, and no live qualified row contradicting the claimed closed world. Raw, archived and quarantined rows are never mapping candidates. Missing/partial or contradictory evidence, duplicate/case variants, an unmapped or unknown account, and a second observed workspace return sanitized fail-closed statuses. The result contains only status, member ID and account ID; no email or token is read or returned. No permissive `complete` flag is accepted.

Final local verification on this packet: `npx vitest run test/slack-account-lookup.test.ts test/identity-resolve.test.ts test/slack-cutover-provenance.test.ts` passed **36/36** across three files. `npm run typecheck` passed; `npx eslint lib/identity/resolve.ts test/slack-account-lookup.test.ts` passed with zero warnings; `npm run lint` exited 0 with 22 warnings in untouched files; `npm run check:docs` passed (67 routes, 99 tables, 8 sources); `git diff --check` and a separate untracked-file trailing-whitespace scan passed. A verified ignored local tar checkpoint retains all three task-owned files at `.context/aio-1170-handoff/slack-account-lookup-checkpoint.tgz`; its SHA-256 is in the adjacent `.sha256` file. These are pure fixture tests: they do not prove the future discovery adapter has enumerated every historical source/account or that its snapshot remains current. The attended migration, coordinated consumer cutover, independent integrated reviews and live acceptance remain pending.

### Inactive Slack workspace observation snapshot reader — September 19, uncommitted packet

GPT-5.6 Sol was the sole implementation writer from clean `a58dac71` on `codex/aio-1170-resume`, continuing the accepted identity cutover specification without a new spec. The user reported Codex weekly usage at 28% at handoff, below the 40% cap; this writer has no fresher utilization readout. The coordinator owns GitHub checkpoints. This packet adds only `lib/identity/slack-workspace-observation-rows.ts`, its real-Postgres data-mechanics suite, and this record. It has no active caller, HTTP route, writer, marker change or runner activation. The writer did not commit, push, edit a PR/Linear ticket, merge or deploy.

`readSlackWorkspaceObservationRows` reads every `slack_workspace_observations` row for one UUID team through one `REPEATABLE READ, READ ONLY` PostgreSQL transaction. It pages by the table's unique UUID primary key with a bounded page size and no total-row cap. It returns only observation ID, team ID, integration ID, exact workspace ID, first-observed timestamp and provenance kind. It queries the observation table directly, preserving records after integration deletion and workspace rotation. Invalid team IDs/page sizes or any page/query failure reject the entire call. The new real-Postgres suite covers team isolation, two workspaces for one integration after deletion, more than 512 rows with tied timestamps, an insert committed after the first page, invalid arguments, later-query failure and read-only enforcement.

The reader supplies recorded `auth.test` facts only. Earlier rotations/deletions can be absent, and the schema's row guard cannot prevent privileged TRUNCATE/DDL or disabled triggers. These rows do not certify a complete historical source census, source account inventory, or cutover readiness. The attended discovery must corroborate historical sources separately and reread/validate relevant evidence under its own cutover transaction.

The writer's isolated Postgres command, `AIOS_DM_RESET=1 npm run test:datamechanics:iso -- test/datamechanics/slack-workspace-observation-rows.datamechanics.test.ts`, exited 126 before any test ran because Docker socket access was denied. The coordinator then ran `AIOS_DM_RESET=1 bash scripts/dm-isolated.sh test/datamechanics/slack-workspace-observation-rows.datamechanics.test.ts` against a freshly recreated disposable PostgreSQL schema: **4/4 passed** at 18:07 ET on September 19. `npm run typecheck`, targeted ESLint with zero warnings, `npm run check:docs` (67 routes, 99 tables, 8 sources), `git diff --check` and a separate trailing-whitespace scan of both new TypeScript files passed. A verified ignored local recovery tar and SHA-256 for the three task-owned files are under `.context/aio-1170-handoff/`. Independent pre-push review found no HIGH or MEDIUM issue; its LOW documentation discrepancy about the real-Postgres result was corrected here before publication.

### Inactive shared Slack credit evidence selector — September 19, uncommitted packet

GPT-5.6 Sol was the sole implementation writer from clean `e327249b` on `codex/aio-1170-resume`, continuing the accepted AIO-1170 specification without a new spec. The user reported weekly Codex usage at 28% at handoff, below the 40% cap; this writer has no fresher authoritative percentage. The coordinator owns GitHub checkpoints. This packet changes only the existing shared credit module, its focused unit tests and this build record. The writer did not commit, push, edit a PR/Linear ticket, merge, deploy, activate cutover or start a runner.

`selectSlackCreditIds` is pure and inactive. Its ledger input distinguishes present, absent and failed reads; failure throws even for a locked item. Its result distinguishes verified-message-ledger-present, structured-participants-present and explicitly partial legacy evidence. An empty or wholly unresolved present ledger prevents participant, version and current-owner fallback; an empty or wholly unresolved structured participant field does the same when the ledger is absent. A correction lock still credits only its corrected human owner, or nobody when cleared. The selector passes worker and owner inputs into the unchanged `creditedContributorIds` and `creditedPrimaryId` helpers, preserves first-seen contributor order and the latest resolved worker, and returns null credit IDs for a caller to omit from `byItem`. It returns IDs only and claims no authored day. Existing active Slack and non-Slack oracle callers are unchanged.

Later integration must read the complete team-scoped verified message ledger and propagate any read error; distinguish a structured participants field's presence from the parsed/resolved list length; resolve accounts through the shared `lookupSlackAccount` with current provenance and exclude connectors; and route both timeline and oracle credit through this selector while omitting null `byItem` entries. This packet does not provide a DB reader, account resolution, cache invalidation, active caller or live Slack proof. Integrated Fable/Astra review and the broader acceptance gates remain pending.

Focused `npx vitest run test/attribution-contributor-credit.test.ts` passed **21/21** after correcting one test expectation to preserve the existing working-owner primary rule. `npm run typecheck`, targeted ESLint on the two changed TypeScript files, `npm run check:docs` (67 routes, 99 tables, 8 sources), and `git diff --check` passed. Full `npm run lint` exited 0 with 22 warnings in untouched repository and ignored handoff files; the changed files have no warnings. These pure tests do not verify the future database read, workspace account lookup, production lock behavior or day projection.

### Inactive team-scoped Slack item credit ledger reader — September 19, uncommitted packet

GPT-5.6 Sol was the sole implementation writer from clean `3b65566e` on `codex/aio-1170-resume`, continuing the accepted AIO-1170 specification without a new spec. The user reported weekly Codex usage at 29% at the last check, below the 40% packet ceiling; this writer has no fresher authoritative percentage. The coordinator owns GitHub checkpoints. This packet adds only `lib/ingest/slack-item-credit-ledger-read.ts`, its real-Postgres data-mechanics suite, and this record. The writer did not commit, push, edit a PR/Linear ticket, merge, deploy, activate cutover or start a runner.

`readSlackItemCreditLedger` accepts one UUID team and up to **512 supplied item IDs** as a list or set; excess input, malformed IDs and page sizes outside **1–512** reject rather than truncate. Duplicate UUIDs are collapsed after the supplied-size check. One `REPEATABLE READ, READ ONLY` transaction pages **all** scoped `slack_messages` rows by their unique UUID primary key, with no total message-row cap. Every requested item returns `absent` only after the complete read found no rows, or `present` when any row exists, including solely deleted, excluded or authorless rows. The author list includes only eligible, nondeleted rows with a canonical raw user ID and an occurrence instant matching the parsed source timestamp; an eligible mismatch rejects the whole call because the table enforces instant nullability but not timestamp equality. Authors carry workspace, channel, message/root timestamps, raw user ID, occurrence instant and root flag, ordered by actual instant then byte-stable source identity. The reader returns no content, token or member ID, grants no access or credit, and has no active caller.

The coordinator's first disposable real-Postgres run, `AIOS_DM_RESET=1 bash scripts/dm-isolated.sh test/datamechanics/slack-item-credit-ledger-read.datamechanics.test.ts`, failed **0/6 passed**. The reader's UUID pattern omitted a UUID group, so valid team IDs were rejected; the requested-ID isolation fixture also reused a unique `(team, workspace, channel, message_ts)` key within one team. This is the initial result, not a passing database verification.

The correction accepts full UUIDs, requires every row for an item (including deleted/excluded rows) to share one workspace, channel and root timestamp, and rejects eligible author IDs outside the shared account lookup's uppercase alphanumeric syntax. Fixtures now use distinct source keys and one canonical root per item. The 519-row pagination fixture uses one valid workspace/channel/root with distinct message timestamps and matching occurrence instants; `.1` and `.100000` are distinct source keys that parse to the same instant and exercise the message-timestamp tie break. New cases reject each source-binding conflict and malformed eligible author IDs. The corrected suite contains ten real-Postgres cases. The writer's isolated rerun exited 126 before test startup because Docker socket access was denied. The coordinator then ran `AIOS_DM_RESET=1 bash scripts/dm-isolated.sh test/datamechanics/slack-item-credit-ledger-read.datamechanics.test.ts` against a freshly recreated disposable PostgreSQL schema: **10/10 passed** at 18:22 ET on September 19. Local `npm run typecheck`, targeted ESLint with zero warnings, `npm run check:docs` (67 routes, 99 tables, 8 sources), shared source-parser/account-lookup unit tests (54/54), `git diff --check`, and an untracked-file trailing-whitespace scan passed. Integrated account mapping, item access, credit selection, cutover and live acceptance remain separate work.

### Inactive Slack credit composition adapter — September 19, uncommitted packet

GPT-5.6 Sol is the sole implementation writer from clean `3aa2198f` on `codex/aio-1170-resume`, continuing the accepted AIO-1170 specification without a new spec. The user's recent weekly Codex reading was 29%, below the 40% packet cap; this writer has no fresher authoritative utilization reading. The coordinator owns GitHub checkpoints. This packet adds only `lib/attribution/slack-credit-composition.ts`, its focused pure unit suite and this record. The writer does not commit, push, edit a PR or Linear ticket, merge, deploy, activate a caller or start a runner.

`composeSlackCreditSelection` accepts one completed `SlackItemCreditLedger` result and requires its item ID to match the supplied item ID. For a present result, it resolves each ordered message author through `lookupSlackAccount` using that message's source workspace as verified provenance. A present empty author list, including a deleted-only ledger, blocks participant/version/owner fallback for an unlocked item. For an absent result, an own `frontmatter.participants` field is present evidence even if empty, malformed or wholly unresolved; valid arrays are ordered by `slackParticipations`. Participant lookup uses only a separately supplied verified item workspace or the shared lookup's completed historical census, inventories and legacy evidence review. A frontmatter workspace, qualified account string or display name does not establish item workspace verification; a frontmatter qualified account without separately verified item workspace remains unresolved. Without ledger or participants, the shared selector reports `legacy_partial` and applies its existing version-then-owner inputs. Every resolved owner, worker and author ID is filtered through a caller-supplied trusted human roster set, so connectors and nonhuman members become null or are excluded. The adapter delegates final contributor, primary and lock decisions to `selectSlackCreditIds`; its return contains only the existing credit kind and IDs or null.

Integration limits: this pure adapter performs no DB read, access check, historical discovery, mapping snapshot refresh, cache invalidation, day projection, active timeline/oracle wiring or read-error handling. A future authorized caller must supply a complete current team-scoped live mapping snapshot and human roster, verified item provenance, and a successfully completed ledger read; it must propagate any ledger read exception instead of constructing `absent`. An unexpected failed/incomplete result supplied at runtime is rejected, not treated as absence. The caller must omit null credit entries from `byItem`. Historical evidence supplied here is evaluated by the shared lookup but this adapter cannot certify that the discovery adapter enumerated the complete past or that a snapshot stayed current. Existing active Slack credit paths still use their old logic. Integrated Fable/Astra review, coordinated consumer cutover and live acceptance remain pending.

Final local verification: `npx vitest run test/slack-credit-composition.test.ts test/attribution-contributor-credit.test.ts test/slack-account-lookup.test.ts test/slack-participants.test.ts` passed **50/50** across four pure suites; `npm run typecheck`, targeted ESLint on the two new TypeScript files with zero warnings, `npm run check:docs` (67 routes, 99 tables, 8 sources), and whitespace checks passed. Full `npm run lint` exited 0 with **22 warnings** in untouched repository/ignored handoff files; the new files have no warnings. These checks do not establish integration with the DB reader or active credit callers.

Independent code review found a **HIGH** provenance conflict, and an independent skeptic confirmed it: the initial adapter accepted a present T1 ledger for an item separately verified as T2, resolving T1 authors before the lock decision. The positive fixture itself supplied those contradictory workspaces. The correction checks every present ledger author's source workspace against `verifiedItemWorkspaceId` before identity resolution or lock handling and throws one static, sanitized `item provenance conflict` error on disagreement, with no fallback. A present ledger with zero eligible authors has no author workspace to compare and remains authoritative empty evidence. For an absent ledger with structured participants, a string `frontmatter.workspace_id` contradicting the separately verified item workspace now raises the same error before participant resolution or lock handling. Frontmatter alone still does not verify workspace. The positive fixture now uses the same verified workspace as its ledger; new unlocked and locked regressions cover both provenance conflicts, and an empty present ledger with a different verified workspace remains valid. The accepted specification is unchanged. The corrected snapshot awaits independent review rerun; this local verification does not close the HIGH review gate.

Correction verification on September 19: the four focused pure suites passed **51/51**; `npm run typecheck`, targeted ESLint on the adapter and suite with zero warnings, `npm run check:docs` (67 routes, 99 tables, 8 sources), and `git diff --check` passed. Full `npm run lint` exited 0 with **22 warnings** in untouched repository/ignored handoff files; the adapter and suite have zero warnings. No DB reader integration, active caller, cutover or live acceptance was exercised. The user reported weekly Codex usage at 30% at the last check, below the 40% cap; the writer has no fresher authoritative percentage. The coordinator still owns commits, GitHub checkpoints, PR/Linear edits, merge and deployment.

### Inactive atomic Slack credit input snapshot — September 19, implementation packet

GPT-5.6 Sol is the sole implementation writer from clean `873e655f` on `codex/aio-1170-resume`, continuing the accepted AIO-1170 specification. The user's most recent weekly Codex reading was 30%, below this packet's 40% cap; this writer has no fresher authoritative percentage. The coordinator owns commits, GitHub checkpoints and independent reviews. This packet changes only the existing item ledger reader, adds `lib/ingest/slack-credit-input-snapshot.ts` and its real-Postgres data-mechanics suite, and updates this record. It adds no active caller, item access decision, historical source census claim, credit decision, cache publication, runner activation or database write.

The existing ledger reader's input validation and message paging are factored into shared functions so standalone reads retain their semantics while the new reader can use them inside one `REPEATABLE READ, READ ONLY` transaction. The new reader accepts one team UUID and at most 512 supplied item UUIDs, and validates page size before opening the transaction. It exhausts all scoped message rows; all team identity rows recognized by the database Slack provider classifier via UUID keyset pages, preserving exact provider and external ID as live mappings; and all current team `kind='human'` and `is_connector=false` member IDs via UUID keyset pages. It reads all three team generation strings from the same snapshot, using zeroes when the pre-activation state row is absent. There is no identity or human total cap, Supabase default limit, email, handle, token, content or secret-bearing projection. Any failed page or invalid eligible ledger row rejects the whole call. A later authorized caller must authorize item IDs and revalidate generations at cache publication.

Local `npm run typecheck`, targeted ESLint with zero warnings, `npm run check:docs` (67 routes, 99 tables, 8 sources), `git diff --check`, and focused pure lookup/composition suites (24/24) passed. Full `npm run lint` exited 0 with 22 warnings in untouched files and ignored handoff copies; changed TypeScript files have zero warnings. The direct data-mechanics command refused to start without `DATABASE_TEST_URL`, as designed. The isolated runner then exited 126 before test startup because this builder sandbox cannot access the Docker daemon socket. A verified ignored local tar checkpoint holds the four task-owned files at `.context/aio-1170-handoff/atomic-credit-input-snapshot.tgz`; the adjacent SHA-256 file verifies it. The coordinator ran `AIOS_DM_RESET=1 bash scripts/dm-isolated.sh test/datamechanics/slack-credit-input-snapshot.datamechanics.test.ts test/datamechanics/slack-item-credit-ledger-read.datamechanics.test.ts` on a freshly recreated disposable PostgreSQL schema. The initial run passed **14/15**: one new fixture asserted an exact one-author array after inserting three messages. The coordinator corrected only that assertion to check all three ordered source timestamps and raw authors, then reran both suites on another fresh schema: **15/15 passed** at 18:38 ET on September 19. The new suite covers all three paged sources, provider variants, connector and agent exclusion, cross-source concurrent commits after the first page, team isolation, generation zero/default behavior, page failures, read-only enforcement and compatibility with the standalone ledger reader. Integrated review, active caller and live acceptance remain pending.

Independent pre-push review of the corrected snapshot diff found no actionable HIGH, MEDIUM or LOW issue. The reviewer confirmed the one read-only repeatable-read transaction, full scoped pagination, failure propagation and standalone reader compatibility. The future caller's item authorization and generation revalidation remain required at activation.

### Inactive factual Slack person-day adapter — September 19, implementation packet

GPT-5.6 Sol was the sole implementation writer from clean `748fc986` on `codex/aio-1170-resume`, continuing the accepted AIO-1170 specification. The user's last reported weekly Codex usage was 31%, below the 40% packet cap; this writer has no fresher authoritative percentage. The coordinator owns GitHub checkpoints and subsequent reviews. This packet changes only `lib/ingest/slack-person-day.ts`, its pure test suite and this record. The writer did not commit, push, edit a PR or Linear ticket, merge, deploy, or activate a caller.

`projectScopedSlackPersonDays` accepts a completed, currently visible team-scoped message array, current live account mapping snapshot, current human roster IDs and per-item selections produced through the existing shared credit selector/composition. It passes every message's own verified workspace and qualified author ID to `lookupSlackAccount`, then delegates UTC grouping, exact instants, counts and order to the existing `projectSlackPersonDays`. It returns only rows whose item has a `verified_message_ledger_present` selection and whose `(sourceItemId, memberId)` belongs to the selected `creditIds.contributorIds`. An absent item selection, null credit, or legacy/participant selection yields no factual message day. A correction lock therefore retains only the corrected owner's actual mapped message days; a cleared lock or an owner with no authored messages yields none. No authored day is reassigned. Malformed selections and incomplete array inputs reject instead of being treated as empty. The projection checks that each persisted UTC instant exactly equals the parsed Slack source timestamp before grouping.

The source visibility boundary remains with the future caller: it must authorize item IDs before `readVisibleSlackMessages`, pass a fully completed read and current team-scoped identity/credit inputs, propagate any read error, then reauthorize visibility and revalidate generations before publication. This pure adapter does no DB read, authorization, generation check, cache write or active timeline/API/UI wiring. It cannot detect a caller that fabricates an empty array after swallowing an upstream failure, or certify that supplied snapshots stayed current.

The independent pre-push reviewer found a HIGH cutover correctness risk: the first adapter version could emit factual message days under legacy or participant credit. A separate skeptic confirmed it, and the coordinator added the verified-ledger guard and both-kind regression. The reviewer also found a MEDIUM risk that an inconsistent persisted instant could shift a day; the coordinator added exact Slack timestamp equality and a regression. Final local verification passed: `npx vitest run test/slack-person-day.test.ts test/slack-credit-composition.test.ts` **28/28**, `npm run typecheck`, targeted ESLint with zero warnings, `npm run check:docs` (67 routes, 99 tables, 8 sources), and `git diff --check`. Full lint in the original builder pass exited 0 with 22 warnings in untouched files. Integrated review and active caller acceptance remain pending.

### Inactive Slack credit batch composition — September 19, implementation packet

GPT-5.6 Sol was the sole implementation writer from clean `83dea69f` on `codex/aio-1170-resume`. The user's latest reported weekly Codex usage was 31%, below the 40% packet cap; this writer has no fresher authoritative reading. The coordinator owns independent review and checkpoints. This packet adds `composeSlackCreditBatch`, binds the existing atomic snapshot result to its canonical lowercase UUID team ID even when its result collections are empty, adds pure regressions and team-binding assertions to the existing database suite, including an uppercase UUID caller. It has no active caller, DB read of its own, authorization decision, cache write or cutover activation.

The pure adapter accepts exactly one completed snapshot and an explicit list of already-authorized Slack item metadata. It validates snapshot shape and team, current mapping/roster shape, complete present/absent ledger variants, UUID item IDs, unique exact ledger/metadata item coverage and item provenance before returning a map. A present ledger with authors requires separately verified item workspace, including when the correction lock is set; present-empty and absent ledgers may omit it because they have no source authors to compare. It invokes `composeSlackCreditSelection` for every item, so present-empty evidence, structured participants, explicit `legacy_partial`, unresolved/nonhuman authors and correction locks retain the shared rule's meaning. Missing ledger rows are errors, never inferred absence; a failed read must propagate instead of being replaced with an absent ledger. The caller must supply verified item source/workspace where required and current authorization, then revalidate visibility and generations before publication. Neither this adapter nor the changed snapshot return certifies historical discovery or snapshot freshness.

Focused pure verification: `npx vitest run test/slack-credit-batch.test.ts test/slack-credit-composition.test.ts test/attribution-contributor-credit.test.ts test/slack-account-lookup.test.ts test/slack-participants.test.ts` passed **59/59** across five files. `npm run typecheck`, targeted ESLint, `npm run check:docs` (67 routes, 99 tables, 8 sources) and `git diff --check` passed. The coordinator reran the final snapshot and standalone ledger suites on a freshly recreated disposable PostgreSQL schema: **15/15 passed**, including populated, empty and uppercase team-ID binding assertions. Independent pre-push review found no actionable issue. Integrated review, active timeline/oracle wiring and live acceptance remain pending.

### Inactive combined Slack evidence snapshot — September 19, implementation packet

GPT-5.6 Sol is the sole implementation writer from clean `c60d21cf` on `codex/aio-1170-resume`. The user's last authoritative weekly Codex usage report was 32%, below the packet's 40% cap; this writer has no fresher percentage. The coordinator owns independent review and checkpoints. This packet adds `readSlackEvidenceSnapshot`, refactors the existing credit-input and visible-message readers into in-session helpers without changing their standalone SQL/order or public return contracts, adds a focused real-Postgres data-mechanics suite and updates this record. There is no active caller, authorization decision, cache write, runner activation, commit, push, PR, merge or deployment in this packet.

The new API validates the team UUID, deduplicated requested item UUIDs, inclusive UTC window and separate credit/message page sizes before one `REPEATABLE READ, READ ONLY` transaction. It captures the validated Date instants so caller mutation cannot change the window between pages. The credit component exhausts per-item present/absent ledgers, current team Slack mappings and human roster, then reads all three generations. The visible-message component uses the same executor and transaction to exhaust eligible, nondeleted rows in that fixed window for exactly those requested IDs. Its seam can run a committed writer after credit inputs and before message pages. Zero item IDs still read mappings, roster and generations and return empty ledgers/messages. Any page failure rejects the whole call; there is no total-row cap or fabricated empty fallback. The existing 512 requested-ID bound and 512 maximum per-page size remain unchanged from the standalone credit reader. The future caller must supply already-authorized item IDs, then reauthorize visibility and revalidate generations before publication; this snapshot does not certify historical discovery or current access.

Local `npm run typecheck`, targeted ESLint with zero warnings, `npm run check:docs` (67 routes, 99 tables, 8 sources), `git diff --check`, and focused pure adapter suites (`test/slack-person-day.test.ts` and `test/slack-credit-batch.test.ts`, **21/21**) passed. The builder's isolated real-Postgres runner exited 126 before test startup because its sandbox could not access the Docker daemon socket. The coordinator reran the final packet on a freshly recreated disposable PostgreSQL schema: the unified snapshot and both standalone reader suites passed **19/19**. The new suite covers a committed message/mapping/generation change between components, team/item/window isolation, credit and visible-message page failures, empty item IDs, and caller mutation of the validated team ID. Independent pre-push review found the mutable-team race; the coordinator fixed it by capturing the team ID before the first await and added a regression. Integrated acceptance remains pending.

### Inactive combined Slack evidence composition — September 19, implementation packet

GPT-5.6 Sol was the sole implementation writer from clean `e8023ec8` on `codex/aio-1170-resume`, under the accepted AIO-1170 specification. The user's reported weekly Codex usage was 33%, below the 40% packet cap; the writer has no fresher authoritative percentage. The coordinator owns GitHub checkpoints and independent review. This packet adds only the pure `composeSlackEvidence` adapter, focused unit tests and this build record. No database read, active caller, authorization decision, cache write, runner activation, commit, push, PR, merge or deployment was performed by the writer.

The adapter accepts one completed atomic `SlackEvidenceSnapshot` and exactly matching already-authorized Slack item metadata. It reuses `composeSlackCreditBatch` for the shared per-item credit selection and `projectScopedSlackPersonDays` for factual UTC day rows. Before projection, every visible message must belong to one supplied item with a present ledger, match an authored ledger row on source key, root, author, instant and root flag, and agree with the item's independently verified workspace. Duplicate source keys across items or within either collection reject. All ledger authors must have valid exact source instants, raw IDs and root flags, including authors outside the visible window; those outside the window need not appear in the visible subset. Present-empty ledgers yield no days, absent ledgers retain participant or legacy credit only, and locks allow only the corrected owner's own authored days. Unresolved, nonhuman and cross-workspace accounts do not earn factual days. Neither locks nor null credit hide malformed message evidence.

The future caller must authorize item IDs before the atomic read, propagate failed reads, then reauthorize visibility and revalidate generations before publishing or caching. The adapter cannot certify a fabricated empty result after a caller swallows a read failure. Focused verification passed: `npx vitest run test/slack-evidence-adapter.test.ts test/slack-credit-batch.test.ts test/slack-person-day.test.ts` **28/28**, `npm run typecheck`, targeted ESLint with zero warnings, `npm run check:docs` (67 routes, 99 tables, 8 sources), `git diff --check`, and an untracked-file trailing-whitespace scan. The unit suite covers multiple items/authors/days, locks and cleared locks, absent/present-empty/legacy evidence, incomplete or mismatched coverage, missing/duplicate/conflicting provenance, and unresolved/nonhuman/cross-team mappings. Integrated review, active caller acceptance and live Slack verification remain pending.

### Inactive Slack source-discovery keyset page — September 19, implementation packet

GPT-5.6 Sol is the sole implementation writer from clean `d42da2b5` on `codex/aio-1170-resume`, under the accepted AIO-1170 specification. The user's last authoritative weekly Codex usage report was 33%, below the 40% packet ceiling; this writer has no fresher percentage. The coordinator owns review and checkpoints. This packet changes only `lib/ingest/slack-source-page-read.ts`, its real-Postgres suite and this record. No active caller, authorization policy, cache/API/UI wiring, schema change, commit, push, PR, Linear edit, merge or deployment was performed by this writer.

`readSlackSourcePage` accepts a team UUID, the complete current authorized visible-item `Set` with no 512-ID input cap, inclusive fixed UTC `since`/`asOf` bounds, page size 1–512 and an optional typed keyset tuple. It validates and captures every input before awaiting the database. One `REPEATABLE READ, READ ONLY` transaction selects only eligible, nondeleted in-window `slack_messages` for those exact visible IDs whose existing same-team item declares `frontmatter.source = 'slack'`. These predicates apply before `GROUP BY` and `LIMIT`. It groups by item ID, orders by the exact microsecond `max(occurred_at)` descending then item UUID descending, and overfetches one group to report `hasMore` and the precise next tuple. The result contains only item IDs, `hasMore` and that tuple; no title, content, source record, total cap or empty-on-error fallback. Continuation generation binding, access rechecks and cursor restart remain future-caller responsibilities. This page alone does not satisfy AC-09's dashboard continuation or day-count proof.

Seven focused real-Postgres cases cover an old root with a recent reply, an invisible newer-message storm before a one-item limit, equal-microsecond tie pagination to exhaustion, team/source/window/deletion/eligibility exclusion, validation and failed/read-only reads, immutable transaction inputs, and more than 512 authorized IDs with bounded one-item pages. The builder's Docker socket and local `initdb` attempts were blocked before test startup. The coordinator ran `AIOS_DM_RESET=1 bash scripts/dm-isolated.sh test/datamechanics/slack-source-page-read.datamechanics.test.ts` against freshly recreated disposable PostgreSQL schemas; the final rerun at 19:12 ET on September 19 passed **7/7** after the builder's fixture-race correction. Local `npm run typecheck`, targeted ESLint with zero warnings, `npm run check:docs` (67 routes, 99 tables, 8 sources) and `git diff --check` passed. Independent pre-push review found no actionable code issue; its LOW documentation mismatch was fixed here. Integrated Fable and final Astra reviews, active timeline wiring and live acceptance remain pending.

### Timeline refresh unit CI environment correction — September 19

The staging-targeted PR's brain-unit job failed three `timeline-panel-refresh` cases before render with `ReferenceError: navigator is not defined` on the CI Node runtime; local Node 25 supplies a global navigator and had masked the test setup gap. The test now stubs `navigator` before its first `react-dom/client` import, and its existing cleanup restores globals after each case. `NODE_OPTIONS=--no-experimental-global-navigator npx vitest run test/timeline-panel-refresh.test.ts` passed **3/3** locally, reproducing CI's missing-global condition. This is a test-host correction; no timeline product code or behavior changed. The PR job must rerun to confirm the GitHub environment.

### CI fixture correction — September 19

Two fixture-only failures were corrected without changing product behavior: the readiness test now ingests a legacy Slack item and uses test-only SQL to model a pre-existing canonical scoped row, preserving the ingress refusal for ordinary scoped writes; the external-costs fixtures use one UTC date derived 30 days before test time so their 90-day read assertions remain in-window.

The coordinator reran both changed files on disposable PostgreSQL: **16/16 passed**. Typecheck, targeted ESLint and `git diff --check` passed. Claude subscription login is now verified (`claude.ai`, Max), and the exact `claude-fable-5-1` model ran twice for a focused review, but both CLI invocations returned empty final review text. Neither is counted as a completed Fable review. The separate staging paired-refresh CI job remains failed on a rollback catalog digest mismatch and requires diagnosis before readiness.

### Inactive Slack source and evidence page — September 19, implementation packet

GPT-5.6 Sol was the implementation writer from clean `bedfaeb9` on `codex/aio-1170-resume`, under the accepted AIO-1170 specification. The coordinator reported weekly Codex usage at 35%, below the 40% ceiling. This packet has no active caller, access decision, cache/API/UI wiring, merge or deployment.

`readSlackSourceEvidencePage` accepts a complete currently authorized visible-item `Set`, fixed inclusive UTC bounds, source page size 1–512 and an optional precise source keyset tuple. It validates and copies caller-owned IDs, team ID, bounds, cursor and page sizes before the first await. One `REPEATABLE READ, READ ONLY` transaction discovers at most 512 source item IDs, then reads exactly those IDs' complete credit ledger and visible message evidence, plus team Slack mappings, human roster and three generations, through the same executor. Source visibility, Slack provenance, eligibility, deletion and time predicates still precede the source limit; existing standalone source-page and evidence-snapshot readers keep their public contracts and SQL/order. The page has no cross-transaction merge or empty-on-error fallback. An empty page still reads mappings, roster and generations.

The future caller must recheck current access and generations before use, bind continuation to data/identity generations, restart pagination when either bound generation changes, and independently verify item source/workspace metadata before factual composition. Source discovery alone does not establish metadata provenance or historical completeness. This packet does not satisfy AC-09's dashboard traversal or AC-11's active access proof.

Local `npm run typecheck`, targeted ESLint with zero warnings, `npm run check:docs` (67 routes, 99 tables, 8 sources), and `git diff --check` passed. The writer's sandbox could not access Docker, and a disposable local `initdb` attempt failed during bootstrap with `shmget: Operation not permitted`. The coordinator then ran the isolated PostgreSQL suite with permitted Docker access. Its first run exposed an inconsistent `$2` parameter type in the new test fixture; after explicit UUID casts, a fresh run passed **18/18** tests across the new source-evidence-page suite and both standalone reader suites. The new cases cover one snapshot across committed message/identity/generation writes, equal-time cursor traversal and exact per-page evidence, more than 512 authorized IDs, empty-page metadata, input capture, failed component pages and read-only enforcement. Claude CLI reported signed out, so the subscription-only Fable review remains unavailable; no API-key fallback was used. Integrated reviews and active/live acceptance remain pending.
