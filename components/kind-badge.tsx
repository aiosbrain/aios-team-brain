/* Kind badge now comes from the shared @aios-alpha/ui design system
   (deliverable→violet, transcript→cyan, decision→amber, task→emerald,
   skill→fuchsia, artifact→neutral). Re-exported here so the existing
   `@/components/kind-badge` import sites keep working unchanged. */
export { KindBadge } from "@aios-alpha/ui";
